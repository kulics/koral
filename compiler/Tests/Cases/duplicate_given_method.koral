// EXPECT-ERROR: Duplicate definition: size

public type Bag(count Int)

given Bag {
    public size(self) Int = self.count
    public size(self) Int = self.count + 1
}

public let main() Void = {
    let b = Bag(5)
    println(b.size().to_string())
}
