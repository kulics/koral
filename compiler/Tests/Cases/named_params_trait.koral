// EXPECT: drawn

trait Drawable {
    draw(self, at_x: Int, at_y: Int) Void
}

type Sprite(id Int)

given Sprite Drawable {
    draw(self, at_x: Int, at_y: Int) Void = println("drawn")
}

let main() Int = {
    let s = Sprite(1)
    s.draw(at_x: 10, at_y: 20)
    return 0
}
