// Test error: non-exhaustive pattern in for loop

// EXPECT_ERROR: exhaustive

let main() Void = {
    let mut list = [Int]List.new()
    list.push(1)
    
    // Literal pattern is not exhaustive
    for 42 = list then {
        println("matched")
    }
}
