// EXPECT: true
// EXPECT: false

// Verifies module-boundary semantics for `given Type Trait`:
// legality is anchored to the Type's root subtree, even when Trait is from std.

type Num(x Int)

given Num {
    equals(self, other Num) Bool = self.x == other.x
}

given Num Eq {
}

let main() Int = {
    let a = Num(1)
    let b = Num(2)
    println(a == a)
    println(a == b)
    yield 0
}
