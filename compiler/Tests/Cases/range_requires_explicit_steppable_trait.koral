// EXPECT-ERROR: Type Num does not explicitly implement trait Steppable

type Num()

given Num Eq {
    equals(self, other Num) Bool = true
}

given Num Ord {
    compare(self, other Num) Int = 0
}

given Num {
    max_value() Self = Num()
    min_value() Self = Num()
    succ(self) [Self]Option = [Self]Option.Some(self)
    pred(self) [Self]Option = [Self]Option.Some(self)
}

let main() Int = {
    let r = Num()..Num()
    for _ in r then {}
    yield 0
}
