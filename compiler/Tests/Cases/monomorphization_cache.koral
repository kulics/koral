// Tests for monomorphization caching - same instantiation should be reused
// EXPECT: 10
// EXPECT: 20
// EXPECT: 30

// Generic function that should be instantiated only once for Int
let [T Any]double_value(x T, y T) T = x

let main() Int = {
    // Multiple calls with same type should use cached instantiation
    let a = double_value(10, 1)
    let b = double_value(20, 2)
    let c = double_value(30, 3)
    
    print_int(a)
    print_int(b)
    print_int(c)
    
    0
}
