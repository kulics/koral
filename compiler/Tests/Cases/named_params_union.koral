// EXPECT: 5

type Shape {
    Circle(radius: Int),
    Rect(width: Int, height: Int),
}

let main() Int = {
    let s = Shape.Circle(radius: 5)
    when s in {
        .Circle(radius: r) then println(r),
        .Rect(width: _, height: _) then println(0),
    }
    return 0
}
