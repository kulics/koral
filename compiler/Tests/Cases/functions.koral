// EXPECT: add:
// EXPECT: 30
// EXPECT: block:
// EXPECT: 20
// EXPECT: inference:
// EXPECT: 30

let add(x Int, y Int) Int = x + y;

let blockExample(a Int, b Int) Int = {
    let result Int = a * b;
    result
};

let testInference() Int = {
    let a = 10;
    let b = 20;
    a + b
};

let main() Int = {
    print_string("add:");
    print_int(add(10, 20));
    
    print_string("block:");
    print_int(blockExample(4, 5));
    
    print_string("inference:");
    print_int(testInference());
    
    0
}
