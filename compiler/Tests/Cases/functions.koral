// EXPECT: add:
// EXPECT: 30
// EXPECT: block:
// EXPECT: 20
// EXPECT: inference:
// EXPECT: 30

let add(x Int, y Int) Int = x + y;

let blockExample(a Int, b Int) Int = {
    let result Int = a * b;
    result
};

let testInference() Int = {
    let a = 10;
    let b = 20;
    a + b
};

let main() Int = {
    printString("add:");
    printInt(add(10, 20));
    
    printString("block:");
    printInt(blockExample(4, 5));
    
    printString("inference:");
    printInt(testInference());
    
    0
}
