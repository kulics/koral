// EXPECT-ERROR: Unexpected token: +

let main() Int = {
    // Continuation is NOT allowed across a comment-only line.
    // The '+' should start a new statement and thus be a parse error.
    let x = 1
    // comment blocks continuation
    + 2

    // Unreachable
    print_int(x)
    0
}
