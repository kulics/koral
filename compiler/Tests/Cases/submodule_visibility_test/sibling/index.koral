// sibling/index.koral - a sibling module
// This module should NOT be able to access child module
// because child is imported as private by the parent

// This should cause a semantic error:
// child is not visible to sibling because parent didn't export it
using super.child

public let sibling_value() Int = 100

// This should fail - child is not accessible
public let try_access_child() Int = child.child_value()
