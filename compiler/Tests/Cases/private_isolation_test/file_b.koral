// file_b.koral - Second file with private symbols
// These private symbols have SAME NAMES as file_a's but should be isolated

// Private variable - same name as in file_a
private let secret_value Int = 222

// Private function - same name as in file_a
private let compute_secret() Int = 2000

// Private type - same name as in file_a
private type SecretData(value Int)

// Public wrapper type to expose private type's value
public type WrapperB(value Int)

// Public accessors to test the private symbols
public let get_private_var_b() Int = secret_value

public let call_private_func_b() Int = compute_secret()

public let create_private_type_b() WrapperB = {
    let secret = SecretData(10)
    WrapperB(secret.value)
}
