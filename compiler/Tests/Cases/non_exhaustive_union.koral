// Test non-exhaustive union pattern matching error
// EXPECT-ERROR: Non-exhaustive match on type 'Color': missing cases .Blue

type Color {
    Red(),
    Green(),
    Blue(),
}

let main() Int = {
    let c = Color.Red()
    
    // Error: Missing .Blue case
    let res = when c is {
        .Red then 1,
        .Green then 2,
    }
    
    yield 0
}
