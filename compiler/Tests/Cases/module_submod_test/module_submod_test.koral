// Test submodule imports
// EXPECT: Module submodule test passed!
using "models" as Models

public let main() Void = {
    // For now, just test that the module system works
    // Full submodule symbol access will be implemented later
    let x = 42
    if x == 42 then {
        println("Module submodule test passed!")
    }
}
