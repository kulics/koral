// Tests that a non-trait given extension on T ref outside std is rejected by orphan rule
// Feature: type-modifier-trait-conformance
// EXPECT-ERROR: Cannot declare

// Non-trait given on T ref is only allowed in std module
given[T Any] T ref {
    public identity(self) T ref = self
}

let main() Void = {
    println("should not compile")
}
