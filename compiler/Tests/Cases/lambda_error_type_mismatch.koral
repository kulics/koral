// Lambda type mismatch error test
// EXPECT-ERROR: Type mismatch

let main() Int = {
    // Error: Lambda return type doesn't match expected type
    // Expected [Int, Int]Func but lambda returns String
    let f [Int, Int]Func = (x) -> "hello"
    
    0
}
