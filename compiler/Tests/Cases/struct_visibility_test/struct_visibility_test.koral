// Test field visibility in struct destructuring and member access
// Private field 'y' of SecretPoint should not be accessible from this file
// EXPECT-ERROR: Cannot access private field 'y' of type 'SecretPoint' in destructuring pattern

using ..."./types"

public let main() Int = {
    let p = SecretPoint(10, 20)
    // This should fail: trying to bind private field 'y' from another file
    if p is SecretPoint(x, y) then {
        println(x)
    }
    yield 0
}
