// EXPECT-ERROR: Cannot take 'ref' of immutable value: c

type Counter(mut value Int)

given Counter {
    increment(self ref) Void = {
        self.value = self.value + 1
    }
}

let main() Int = {
    let c = Counter(1)
    c.increment()
    yield 0
}
