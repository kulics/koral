// EXPECT: lowercase_member_ok

public using "tools" as Tools
using "worker" as Worker
using Worker.run

let main() Int = {
    let _ = if run() == 41 then println("lowercase_member_ok")
            else println("lowercase_member_bad");
    yield 0
}
