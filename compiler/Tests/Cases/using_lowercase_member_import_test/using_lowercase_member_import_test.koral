// EXPECT: lowercase_member_ok

public using self.tools
using self.worker

let main() Int = {
    let _ = if worker.run() == 41 then println("lowercase_member_ok")
            else println("lowercase_member_bad");
    yield 0
}
