// EXPECT-ERROR: does not explicitly implement trait Ord

type Score(v Int)

given Score {
    compare(self, other Score) Int = self.v - other.v
}

let main() Int = {
    let a = Score(1)
    let b = Score(2)
    let _ = a < b
    yield 0
}
