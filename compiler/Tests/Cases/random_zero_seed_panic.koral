// Test that from_seed(0, 0, 0, 0) triggers panic
// Validates: Requirements 3.3

// EXPECT: Panic: SimpleRandomSource: seed must not be all zeros

using std.random.*

let main() Void = {
    let rng = SimpleRandomSource.from_seed(0, 0, 0, 0)
    print_line(rng.generate())
}
