// Test basic type alias functionality
// EXPECT: meters_test:
// EXPECT: 42
// EXPECT: alias_chain:
// EXPECT: 100
// EXPECT: alias_struct:
// EXPECT: 10
// EXPECT: alias_func_param:
// EXPECT: 7

// Basic type alias
type Meters = Int

// Alias chain: A -> B -> Int
type Distance = Meters

// Struct for testing
type Point(x Int, y Int)

// Alias to struct
type Coord = Point

// Alias used in function parameter and return type
let add_meters(a Meters, b Meters) Meters = a + b

let main() Int = {
    // Use alias for variable
    let m Meters = 42
    print_line("meters_test:")
    print_line(m)

    // Use alias chain
    let d Distance = 100
    print_line("alias_chain:")
    print_line(d)

    // Use alias to struct
    let c = Coord(10, 20)
    print_line("alias_struct:")
    print_line(c.x)

    // Use alias in function
    let result = add_meters(3, 4)
    print_line("alias_func_param:")
    print_line(result)

    0
}
