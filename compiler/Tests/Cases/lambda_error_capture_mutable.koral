// Lambda capture mutable variable error test
// EXPECT-ERROR: Cannot capture mutable variable

let main() Int = {
    // Error: Cannot capture mutable variable
    let mut x = 10
    let f [Int]Func = () -> x + 1  // Error: x is mutable
    
    yield 0
}
