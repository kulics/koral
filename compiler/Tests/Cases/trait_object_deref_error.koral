// Test deref trait object compile error
// EXPECT-ERROR: Cannot dereference a trait object reference

trait Greet {
    greet(self) String
}

type Greeter(name String)

given Greeter {
    public greet(self) String = "hello"
}

let main() Int = {
    let g1 = ref Greeter("test")
    let g Greet ref = g1
    let v = deref g
    0
}
