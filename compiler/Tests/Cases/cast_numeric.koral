let main() Void = {
    let x Float64 = 42.0
    let y Int = (Int)x
    print_int(y)

    let a Int = 255
    let b UInt8 = (UInt8)a
    let c Int = (Int)b
    print_int(c)
}

// EXPECT: 42
// EXPECT: 255
