// EXPECT: 7

type LocalSecret(private value Int)

given LocalSecret {
    public get(self) Int = self.value
}

let main() Int = {
    let s = LocalSecret(7)
    println(s.get().to_string())
    yield 0
}
