// EXPECT: true
// EXPECT: false

trait MyEq {
    equals(self, other Self) Bool
}

given MyEq {
    not_equals(self, other Self) Bool = not self.equals(other)
}

type Num(x Int)

given Num {
    equals(self, other Num) Bool = self.x == other.x
}

given Num MyEq {
}

let main() Int = {
    let a = Num(1)
    let b = Num(2)
    println(a.not_equals(b))
    println(a.not_equals(a))
    yield 0
}
