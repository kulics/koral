// Test unreachable pattern detection
// This file contains patterns that should trigger compile errors
// EXPECT-ERROR: Unreachable pattern

type Status {
    Active(),
    Inactive(),
}

let main() Int = {
    let s = Status.Active()
    
    // Error: Pattern after wildcard is unreachable
    let res = when s is {
        _ then 1,
        .Active then 2,  // This should be unreachable
    }
    
    0
}
