// Lambda type inference failure error test
// EXPECT-ERROR: Cannot infer type

let main() Int = {
    // Error: Cannot infer parameter type without context
    let f = (x) -> x + 1  // Error: no type annotation for x and no expected type
    
    0
}
