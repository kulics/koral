// EXPECT: true
// EXPECT: true
// EXPECT: true

let main() Int = {
    let b1 Bool = true
    let b2 Bool = true
    print_line(b1.hash() == b2.hash())

    let s1 = "abc"
    let s2 = "abc"
    print_line(s1.hash() == s2.hash())

    // Length-based hash: different-length strings must not collide here.
    let a = "a"
    let ab = "ab"
    print_line(a.hash() <> ab.hash())

    yield 0
}
