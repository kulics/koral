// EXPECT-ERROR: Type 'LocalThing' does not belong to module 'Alpha'

using Self.Alpha

type LocalThing(value Int)

let main() Int = {
    let value Alpha.LocalThing = LocalThing(1)
    yield value.value
}
