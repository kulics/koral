// EXPECT-ERROR: does not explicitly implement trait [Int]Iterator

type MyIter()
type MyRange()

given MyIter {
    next(self ref) [Int]Option = Option.None()
}

given MyRange {
    iterator(self ref) MyIter = MyIter()
}

let main() Int = {
    let r = MyRange()
    for n in r then {
        let _ = n
    }
    yield 0
}
