// EXPECT-ERROR: does not explicitly implement trait ToString

type User(name String)

given User {
    to_string(self) String = self.name
}

let main() Int = {
    let u = User("koral")
    println("user=\(u)")
    yield 0
}
