// private using 只应在声明文件内可见，不应泄漏到同模块其他文件
// EXPECT-ERROR: Use 'time.DateTime' to access it.

using "imports"
using "consumer"

public let main() Int = {
	let _ = trigger_private_using_leak()
	yield 0
}
