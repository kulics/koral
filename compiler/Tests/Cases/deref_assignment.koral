// EXPECT-ERROR: Invalid operation assignment target between types deref

public let main() Void = {
    let mut x = 1;
    let r = ref x;

    // Intentionally forbidden: `deref` cannot be an assignment target.
    deref r = 2;
}
