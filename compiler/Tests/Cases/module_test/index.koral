// Test module system - entry file
using "utils"

public let main() Void = {
    let result = helper_add(1, 2)
    // Just use the result to avoid unused warning
    if result == 3 then {
        // success
    }
}
