let main() Void = {
    let p = [Int]alloc_memory(1)
    init_memory(p, 42)

    let u UInt = (UInt)p
    let p2 Int ptr = (Int ptr)u

    print_line(deptr p2)
    dealloc_memory(p2)
}

// EXPECT: 42
