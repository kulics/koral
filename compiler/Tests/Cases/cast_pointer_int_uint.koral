let main() Void = {
    let p = [Int]alloc_memory(1);
    p.init(42);

    let u UInt = (UInt)p;
    let p2 [Int]Pointer = ([Int]Pointer)u;

    print_int(deref p2.peek());
    [Int]dealloc_memory(p2);
};

// EXPECT: 42
