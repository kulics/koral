// EXPECT-ERROR: Invalid operation assignment target

type Box(val Int ref)

let getBox() Box = {
    let r Int ref = ref 100
    yield Box(r)
}

let main() Int = {
    let b = getBox()
    println(deref b.val)
    yield 0
}
