// EXPECT-ERROR: Invalid operation assignment target

type Box(inner Int ref)

let getBox() Box = {
    let r Int ref = 100.ref
    yield Box(r)
}

let main() Int = {
    let b = getBox()
    println(b.inner.val)
    yield 0
}
