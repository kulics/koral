// EXPECT-ERROR: Type mismatch: expected Int16, got Int8

let main() Void = {
    let a Int8 = (Int8)1;

    // No implicit widening: must write `(Int16)a` explicitly.
    let b Int16 = a;
    print_int((Int)b);
};
