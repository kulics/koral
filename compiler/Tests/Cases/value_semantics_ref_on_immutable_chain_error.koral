// EXPECT-ERROR: Cannot take mutable reference from immutable binding 'vec'

type Vec(mut x Int)
type Nest(mut vec Vec)

let main() Int = {
    let n = Nest(Vec(1))
    let _ Vec mut ref = n.vec.ref
    yield 0
}
