type [T, U] Pair(first T, second U);

let [T] duplicate(x T) [T, T]Pair = {
    [T, T]Pair(x, x)
};

type Point(x Int, y Int);

let main() = {
    let p1 = duplicate(100);
    print_int(p1.first);
    print_int(p1.second);

    let pt = Point(10, 20);
    let p2 = duplicate(pt);
    print_int(p2.first.x);
    print_int(p2.second.y);

    let p = ref 123;
    let p3 = duplicate(p);
    print_int(deref p3.first);
    print_int(deref p3.second);
}
// EXPECT: 100
// EXPECT: 100
// EXPECT: 10
// EXPECT: 20
// EXPECT: 123
// EXPECT: 123