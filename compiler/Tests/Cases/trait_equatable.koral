// EXPECT: true
// EXPECT: false

type Num(x Int)

given Num {
    __equals(self, other Num) Bool = {
        self.x == other.x
    }
}

let [T Equatable] isEqual(a T, b T) Bool = a == b

let main() Int = {
    print_line(isEqual(Num(1), Num(1)))
    print_line(isEqual(Num(1), Num(2)))
    0
}
