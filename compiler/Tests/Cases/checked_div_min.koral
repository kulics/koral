// Test that signed MIN_VALUE / -1 triggers panic (result would overflow)
// EXPECT: Panic: integer overflow in division

let main() Void = {
    let a Int8 = -128
    let b Int8 = -1
    let c = a / b
    print_line(c)
}
