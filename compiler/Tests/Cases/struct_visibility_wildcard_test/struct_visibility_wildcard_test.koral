// Test that wildcard on private fields is allowed in destructuring
// Using _ for private field should work fine
// EXPECT: x_value: 10
// EXPECT: Wildcard on private field works!

using "types"

public let main() Int = {
    let p = SecretPoint(10, 20)
    // This should succeed: using wildcard for private field 'y'
    if p is SecretPoint(x, _) then {
        print("x_value: ")
        print_line(x)
    }
    print_line("Wildcard on private field works!")
    0
}
