// EXPECT: nested_member_import_ok
using Std.Time.DateTime

let main() Int = {
    let dt = DateTime.from_unix_seconds(0)
    let _ = if dt.to_unix_seconds() == 0 then println("nested_member_import_ok")
            else println("nested_member_import_bad")
    yield 0
}
