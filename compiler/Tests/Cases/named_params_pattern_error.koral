// EXPECT-ERROR: Missing named argument label in pattern

type Shape {
    Rect(width: Int, height: Int),
}

let main() Int = {
    let s = Shape.Rect(width: 10, height: 20)
    when s in {
        .Rect(10, height: h) then println(h),
    }
    return 0
}