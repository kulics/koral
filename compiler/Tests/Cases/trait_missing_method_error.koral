// EXPECT-ERROR: missing method equals

type Box(x Int)

let [T Equality] isSelfEqual(x T) Bool = x == x

let main() Int = {
    let b = Box(1)
    let ok = isSelfEqual(b)
    if ok then { 1 } else { 0 }
}
