// EXPECT-ERROR: compiler protocol method __drop cannot be called explicitly

let main() Int = {
    let a = "a"
    // Compiler protocol methods (like __drop) must not be callable explicitly.
    a.__drop()
    yield 0
}
