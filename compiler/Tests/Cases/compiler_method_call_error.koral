// EXPECT-ERROR: compiler protocol method __equals cannot be called explicitly

let main() Int = {
    let a = "a";
    let b = "b";

    // Compiler protocol methods (like __equals) must not be callable explicitly.
    let ok = a.__equals(b);
    if ok then { 1 } else { 0 }
}
