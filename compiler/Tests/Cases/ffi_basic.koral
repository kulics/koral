foreign using "stdio.h"
foreign using "math.h"

foreign type FILE
foreign let fclose(file FILE) Int32
foreign let sin(x Float64) Float64
foreign let exit(status Int32) Never

let main() = {
    let mut v Float64 = sin(0.0)
    v = sin(v)
}
