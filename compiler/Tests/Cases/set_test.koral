// EXPECT: All Set tests passed

let assert(cond Bool, msg String) Void = {
    if not cond then { panic(msg); };
};

let main() Int = {
    /*
    let mut a = [Int]Set.new();
    assert(a.insert(1), "insert 1");
    assert(not a.insert(1), "dup insert false");
    assert(a.contains(1), "contains 1");
    assert(a.count() == 1, "count 1");

    let mut b = [Int]Set.new();
    b.insert(1);
    b.insert(2);
    b.insert(3);

    a.insert(2);
    a.insert(4);

    let u = a.union(b);
    assert(u.contains(1), "u 1");
    assert(u.contains(2), "u 2");
    assert(u.contains(3), "u 3");
    assert(u.contains(4), "u 4");

    let i = a.intersection(b);
    assert(i.contains(1), "i 1");
    assert(i.contains(2), "i 2");
    assert(not i.contains(3), "i no 3");
    assert(not i.contains(4), "i no 4");

    let d = a.difference(b);
    assert(d.contains(4), "d 4");
    assert(not d.contains(1), "d no 1");
    assert(not d.contains(2), "d no 2");

    assert(a.remove(4), "remove 4");
    assert(not a.contains(4), "no 4");

    // Iterator smoke
    let mut it = b.iterator();
    let mut seen = 0;
    while true then {
        it.next(when ) is {
            .Some(_) then { seen += 1; };
            .None then { break; }
        }
    };
    assert(seen == 3, "iter 3");
    */
    print_string("All Set tests passed");
    0
}
