// EXPECT-ERROR: compiler protocol method drop cannot be called explicitly

type Token()

given Token Drop {
    drop(self ref) Void = {}
}

let main() Int = {
    let mut a = Token()
    a.drop()
    yield 0
}
