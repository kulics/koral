// EXPECT: ok

foreign type Stat(st_mode UInt32, st_size Int64)

given Stat {
    new() Stat = Stat((UInt32)0, (Int64)0)
}

let main() = {
    let mut st = Stat.new()
    st.st_mode = (UInt32)1
    let st_ptr Stat ptr = ptr st
    let size = st_ptr.st_size
    if size >= (Int64)0 then { println("ok") } else { println("ok") }
}
