// Tests for declaration-time validation of generic unions
// EXPECT-ERROR: Undefined type

// Test: Generic union with undefined type in case parameter
type [T Any]BadUnion {
    Good(value T),
    Bad(value UndefinedType),
}

let main() Int = {
    yield 0
}
