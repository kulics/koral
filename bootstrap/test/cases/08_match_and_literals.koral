let empty = [];
let numbers = [1, 2, 3];
let lookup = [1: 10, 2: 20];

let m1 = when n in {
    0 then 100,
    true then 1,
    _ then 999,
};

let m2 = when opt in {
    .Some(v) then v,
    .None then 0,
};
