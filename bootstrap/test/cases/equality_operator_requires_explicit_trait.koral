// EXPECT-ERROR: does not explicitly implement trait Eq

type Value(x Int)

given Value {
    equals(self, other Value) Bool = self.x == other.x
}

let main() Int = {
    let a = Value(1)
    let b = Value(1)
    let _ = a == b
    yield 0
}
