// EXPECT-ERROR: Parent trait 'Parent' must be explicitly implemented for child trait 'Child'

trait Parent {
    parent(self) Int
}

trait Child Parent {
    child(self) Int
}

type Demo(v Int)

given Demo Child {
    child(self) Int = self.v
}

let main() Int = {
    let _ = Demo(1)
    yield 0
}
