// EXPECT-ERROR: does not explicitly implement trait Mul

type Scalar(v Int)

given Scalar {
    mul(self, other Scalar) Scalar = Scalar(self.v * other.v)
}

let main() Int = {
    let a = Scalar(2)
    let b = Scalar(3)
    let _ = a * b
    yield 0
}
