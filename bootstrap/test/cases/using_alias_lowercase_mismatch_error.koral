// EXPECT-ERROR: alias must start with a lowercase letter
// Note: This test validates that using alias case must match the referenced identifier.
// With string syntax, the alias for a submodule must start with uppercase.
// This test uses the old identifier-based syntax where 'tools' starts lowercase
// but alias 'Tools' starts uppercase, which is a mismatch.

using tools as Tools

let main() Int = 0
