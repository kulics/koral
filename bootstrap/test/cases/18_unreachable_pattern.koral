// Test: unreachable pattern after wildcard

// EXPECT-ERROR: Unreachable pattern: pattern after wildcard/binding is unreachable

type Status {
    Active(),
    Inactive(),
    Pending(),
}

foreign let s Status

let result = when s in {
    .Active() then 1,
    _ then 0,
    .Inactive() then 2,
}
