// Test: exhaustiveness errors - missing cases in when expressions

// EXPECT-ERROR: Non-exhaustive patterns for type 'Shape'. Missing cases: Circle, Triangle

type Shape {
    Circle(),
    Square(),
    Triangle(),
}

let shape_name(s Shape) String = {
    return when s in {
        .Square() then "square",
    }
}
