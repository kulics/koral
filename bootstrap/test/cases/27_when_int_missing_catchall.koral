// Test: when expression on Int without catchall

// EXPECT-ERROR: Non-exhaustive patterns for type 'Int': add a wildcard or binding pattern

foreign let n Int

let result = when n in {
    0 then "zero",
    1 then "one",
}
