// Test trait object self-conformance: ToString ref can be passed to [T ToString]
// EXPECT: hello world

let [T ToString]show(a T ref) String = a.to_string()

let main() Int = {
    let mut value = "hello world"
    let s1 = value.ref
    let s ToString ref = s1
    println(show(s))
    yield 0
}
