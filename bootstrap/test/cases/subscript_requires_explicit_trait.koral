// EXPECT-ERROR: does not explicitly implement trait [Int, Int]Index

type Box(v Int)

given Box {
    at(self, idx Int) Int = self.v
}

let main() Int = {
    let b = Box(7)
    let _ = b[0]
    yield 0
}
