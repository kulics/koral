// Test: generic types and functions

type [T]Box(value T)
type [A, B]Pair(first A, second B)

type [T]Stack(
    items [T]List,
)

trait [T]Container {
    get(self) T
}

given [T] [T]Box [T]Container {
    get(self) T = self.value
}

// 泛型函数
let [T]identity(x T) T = x

let [A, B]swap(p [A, B]Pair) [B, A]Pair = {
    return [B, A]Pair(p.second, p.first)
}

// 使用泛型类型（类型推断）
let int_box = Box(42)
let str_box = Box("hello")
let mixed_pair = Pair(1, "one")

// 嵌套泛型
let nested = Box(Box(100))
