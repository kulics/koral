// EXPECT: result 9
// EXPECT: Drop 9

type Tracker(id Int)

given Tracker Drop {
    drop(self ref) = {
        println("Drop \(self.id)")
    }
}

let identity(x Tracker) Tracker = x

let main() Int = {
    let result = identity(Tracker(9))
    println("result \(result.id)")
    yield 0
}