// EXPECT: true
// EXPECT: true
// EXPECT: true

let main() Int = {
    let b1 Bool = true
    let b2 Bool = true
    println(b1.hash() == b2.hash())

    let s1 = "abc"
    let s2 = "abc"
    println(s1.hash() == s2.hash())

    // Length-based hash: different-length strings must not collide here.
    let a = "a"
    let ab = "ab"
    println(a.hash() <> ab.hash())

    yield 0
}
