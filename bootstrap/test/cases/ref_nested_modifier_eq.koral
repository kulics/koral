// Tests that T ref ref satisfies Eq when T implements Eq (recursive blanket given check)
// Feature: type-modifier-trait-conformance

type Value(n Int)

given Value Eq {
    public equals(self, other Value) Bool = self.n == other.n
}

let [T Eq] are_equal(a T, b T) Bool = a == b

let main() Void = {
    let mut v1 = Value(7)
    let mut v2 = Value(7)
    let inner1 = v1.ref
    let inner2 = v2.ref
    let mut r1 = inner1
    let mut r2 = inner2
    let a = r1.ref
    let b = r2.ref
    println(are_equal(a, b))
}
// EXPECT: true
