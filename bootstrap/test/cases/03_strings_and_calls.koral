let simple = "hello";
let interp = "hello \(name), value=\(1 + 2)";
let multiline = """
line1
line2 \(name)
""";

let nested_let = let x = 1 then add(x, 2);
let call_chain = foo(bar(1, 2), baz("x"));
