// Test: trait conformance error - parameter count mismatch

// EXPECT-ERROR: expected 1 params, got 2

trait Transformer {
    transform(self, input Int) Int
}

type Doubler()

// transform 实现了额外的参数
given Doubler Transformer {
    transform(self, input Int, extra Int) Int = input * 2
}
