// EXPECT-ERROR: Type 'LocalBox' does not belong to module 'Alpha'

using "alpha" as Alpha

type [T Any]LocalBox(value T)

let main() Int = {
    let value Alpha.[Int]LocalBox = [Int]LocalBox(1)
    yield value.value
}
