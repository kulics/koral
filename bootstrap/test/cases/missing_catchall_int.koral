// Test missing catchall pattern for Int type
// EXPECT-ERROR: requires a wildcard or variable binding

let main() Int = {
    let n = 42
    
    // Error: Int has infinite domain, needs catchall
    let res = when n in {
        1 then 10,
        2 then 20,
        3 then 30,
    }
    
    yield 0
}
