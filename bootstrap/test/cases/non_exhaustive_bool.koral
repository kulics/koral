// Test non-exhaustive bool pattern matching error
// EXPECT-ERROR: Non-exhaustive match on type 'Bool': missing cases false

let main() Int = {
    let b = true
    
    // Error: Missing false case
    let res = when b in {
        true then 1,
    }
    
    yield 0
}
