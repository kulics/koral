public type SourceSpan(
    start SourceLocation,
    end SourceLocation,
)

public let unknown_span() SourceSpan = SourceSpan(unknown_location(), unknown_location())

public let is_known_span(span SourceSpan) Bool = {
    return is_known_location(span.start) and is_known_location(span.end)
}
