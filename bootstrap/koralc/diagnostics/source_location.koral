public type SourceLocation(
    line UInt,
    column UInt,
)

public let unknown_location() SourceLocation = SourceLocation(0, 0)

public let is_known_location(loc SourceLocation) Bool = {
    return loc.line > 0 and loc.column > 0
}
