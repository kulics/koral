// 表达式求值器 - Environment 组件
// 用于存储变量名到值的映射
// Note: Value type is imported via the main module


public type Environment(mutable vars Dict[String, Value])

given Environment {
    public new() Self = Environment(Dict[String, Value].new())

    public get(*self, name String) Option[Value] = self.vars.get(name)

    public set(*mutable self, name String, value Value) Void = {
        self.vars.insert(name, value)
    }

    public clear(*mutable self) Void = self.vars.clear()

    public list_vars(*self) Void = {
        if self.vars.is_empty() then {
            println("No variables defined.")
        } else {
            println("Variables:")
            for entry in self.vars then {
                println("  \(entry.first) = \(entry.second.to_string())")
            }
        }
    }
}
