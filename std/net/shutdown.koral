// ============================================================================
// std.net - Shutdown Enum
// ============================================================================
// Provides: Shutdown
// Access via: using std.net
// ============================================================================

/// Shutdown mode for socket connections.
/// Maps to system call values: SHUT_RD(0), SHUT_WR(1), SHUT_RDWR(2).
public type Shutdown {
    Read(),
    Write(),
    Both(),
}
