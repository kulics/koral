// ============================================================================
// std.io - SeekOrigin Enum
// ============================================================================
// Seek origin enum for use with the Seeker trait.
// Specifies the reference point and offset for seek operations.
// ============================================================================

public type SeekOrigin {
    Start(offset UInt64),
    End(offset Int64),
    Current(offset Int64),
}
