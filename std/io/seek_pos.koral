// ============================================================================
// std.io - SeekPos Enum
// ============================================================================
// Seek position enum for use with the Seeker trait.
// Specifies the reference point and offset for seek operations.
// ============================================================================

public type SeekPos {
    Start(offset UInt64),
    End(offset Int64),
    Current(offset Int64),
}
