// ============================================================================
// Koral Standard Library - Set Type
// ============================================================================
// NOTE: This file is merged into core.koral, so all previous types are already available.
// ============================================================================

// ============================================================================
// Set Type Definition
// ============================================================================
type SetBucket[T Hash] {
    Empty(),
    Occupied(value T),
    Deleted(),
};

public type mutable Set[T Hash](module_private mutable buckets *unsafe mutable SetBucket[T], module_private mutable size UInt, module_private mutable capacity UInt);

given[T Hash] Set[T] as Drop {

    drop(self) Void = {
        for i in 0..<self.capacity then {
            deinit_memory(self.buckets + i);
        };
        dealloc_memory(self.buckets);
    };
};

public type mutable SetIterator[T Hash](module_private source Set[T], module_private mutable index UInt);

// ============================================================================
// Set Methods
// ============================================================================
given[T Hash] Set[T] {

    public new() Self = {
        let capacity UInt = 16;
        let buckets = alloc_memory[SetBucket[T]](capacity);
        for i in 0..<capacity then {
            init_memory(buckets + i, SetBucket[T].Empty());
        };
        return Set[T](buckets, 0, capacity);
    };

    public with_capacity(capacity UInt) Self = {
        // Hash tables need extra space for efficiency (load factor ~0.5)
        let cap = if capacity < 4 then 4 else capacity * 2;
        let buckets = alloc_memory[SetBucket[T]](cap);
        for i in 0..<cap then {
            init_memory(buckets + i, SetBucket[T].Empty());
        };
        return Set[T](buckets, 0, cap);
    };

    public count(self) UInt = self.size;

    // Using when for bucket handling
    file_private find_slot(self, value T) Option[UInt] = {
        let h = value.hash();
        let mutable idx = h % self.capacity;
        let mutable first_deleted = Option[UInt].None();
        let mutable probes UInt = 0;
        while probes < self.capacity then {
            let bucket = self.buckets[idx];
            when bucket in {
                .Empty() then {
                    when first_deleted in {
                        .Some(_) then {
                            return first_deleted;
                        },
                        .None() then {
                            return Option[UInt].Some(idx);
                        },
                    };
                },
                .Deleted() then {
                    when first_deleted in {
                        .None() then {
                            first_deleted = Option[UInt].Some(idx);
                        },
                        _ then {},
                    };
                },
                .Occupied(v) then {
                    if v == value then {
                        return Option[UInt].Some(idx);
                    };
                },
            };
            idx = (idx + 1) % self.capacity;
            probes += 1;
        };
        return first_deleted;
    };

    file_private rehash(self) Void = {
        let old_cap = self.capacity;
        let old_buckets = self.buckets;
        let new_cap = old_cap * 2(UInt);
        let new_buckets = alloc_memory[SetBucket[T]](new_cap);
        for i in 0..<new_cap then {
            init_memory(new_buckets + i, SetBucket[T].Empty());
        };
        self.buckets = new_buckets;
        self.capacity = new_cap;
        self.size = 0;
        for i in 0..<old_cap then {
            let bucket = take_memory(old_buckets + i);
            when bucket in {
                .Occupied(v) then {
                    self.insert(v);
                },
                _ then {},
            };
        };
        dealloc_memory(old_buckets);
    };

    public insert(self, value T) Void = {
        let _ = self.try_insert(value);
    };

    public try_insert(self, value T) Bool = {
        if self.size * 2 >= self.capacity then {
            self.rehash();
        };
        when self.find_slot(value) in {
            .None() then {
                panic("Set insert failed");
            },
            .Some(idx) then {
                let bucket = self.buckets[idx];
                when bucket in {
                    .Occupied(_) then {
                        return false;
                    },
                    .Empty() or .Deleted() then {
                        self.buckets[idx] = SetBucket[T].Occupied(value);
                        self.size = self.size + 1;
                        return true;
                    },
                };
            },
        };
    };

    public insert_set(self, other Set[T]) Void = {
        for v in other then {
            self.insert(v);
        };
    };

    public contains(self, value T) Bool = {
        when self.find_slot(value) in {
            .None() then {
                return false;
            },
            .Some(idx) then {
                let bucket = self.buckets[idx];
                return when bucket in {
                    .Occupied(_) then true,
                    _ then false,
                };
            },
        };
    };

    public remove(self, value T) Void = {
        let _ = self.try_remove(value);
    };

    public try_remove(self, value T) Bool = {
        when self.find_slot(value) in {
            .None() then {
                return false;
            },
            .Some(idx) then {
                let bucket = self.buckets[idx];
                return when bucket in {
                    .Occupied(_) then {
                        self.buckets[idx] = SetBucket[T].Deleted();
                        self.size = self.size - 1;
                        return true;
                    },
                    _ then false,
                };
            },
        };
    };

    public is_empty(self) Bool = self.size == 0;

    public is_subset_of(self, other Set[T]) Bool = {
        for v in self then {
            if not other.contains(v) then {
                return false;
            };
        };
        return true;
    };

    public is_superset_of(self, other Set[T]) Bool = other.is_subset_of(self);

    public clear(self) Void = {
        for i in 0..<self.capacity then {
            self.buckets[i] = SetBucket[T].Empty();
        };
        self.size = 0;
    };

    // Retains only elements satisfying the predicate. O(n).
    public retain(self, predicate Func(T) Bool) Void = {
        for i in 0..<self.capacity then {
            let bucket = self.buckets[i];
            when bucket in {
                .Occupied(v) then {
                    if not predicate(v) then {
                        self.buckets[i] = SetBucket[T].Deleted();
                        self.size = self.size - 1;
                    };
                },
                _ then {},
            };
        };
    };

    // Using while is pattern matching for iterator loops
    public union(self, other Set[T]) Set[T] = {
        let mutable result = Set[T].new();
        for v in self then {
            result.insert(v);
        };
        for v in other then {
            result.insert(v);
        };
        return result;
    };

    public intersection(self, other Set[T]) Set[T] = {
        let mutable result = Set[T].new();
        for v in self then {
            if other.contains(v) then {
                result.insert(v);
            };
        };
        return result;
    };

    public difference(self, other Set[T]) Set[T] = {
        let mutable result = Set[T].new();
        for v in self then {
            if not other.contains(v) then {
                result.insert(v);
            };
        };
        return result;
    };

    public symmetric_difference(self, other Set[T]) Set[T] = {
        let mutable result = Set[T].new();
        for v in self then {
            if not other.contains(v) then {
                result.insert(v);
            };
        };
        for v in other then {
            if not self.contains(v) then {
                result.insert(v);
            };
        };

        return result;
    };
};

given[T Hash] Set[T] as Default {

    public default() Self = Set[T].new();
};

given[T Hash] Set[T] as Iterable[T, SetIterator[T]] {

    public iterator(self) SetIterator[T] = SetIterator[T](self, 0);
};

// ============================================================================
// Set Iterator
// ============================================================================
given[T Hash] SetIterator[T] as Iterator[T] {

    public next(self) Option[T] = {
        while self.index < self.source.capacity then {
            let bucket = self.source.buckets[self.index];
            self.index = self.index + 1;
            when bucket in {
                .Occupied(v) then {
                    return Option[T].Some(v);
                },
                _ then {},
            };
        };
        return Option[T].None();
    };
};
