// Koral Language Grammar (BNF)
// Updated to match current implementation (Lexer/Parser)

<program> ::= <using-decl>* <global-decl>*

// using 必须出现在其他全局声明之前

<using-decl> ::= "using" <using-spec> ";"?
<using-spec> ::= <file-merge-spec>
               | <module-using-spec>

<file-merge-spec> ::= <string-literal>
// 说明：
// 1) using "path"：把相对路径对应的 .koral 文件合并进当前模块
// 2) 路径相对于当前文件所在目录解析，可包含 "." / ".."
// 3) 文件合并不会创建子模块、别名或导出面

<module-using-spec> ::= <module-name> "{" <module-import-list> "}"
<module-name> ::= <identifier> ("::" <identifier>)*

<module-import-list> ::= ".."
                       | <module-import-item> ("," <module-import-item>)* ","?

<module-import-item> ::= <identifier> <module-item-alias>?
<module-item-alias> ::= "as" <identifier>

// 说明：
// 1) using std::io { Reader }：从显式模块导入当前可见符号（public；同 package 时也可导入 protected public）
// 2) using std::io { Reader as IoReader, Writer }：支持逐项别名
// 3) using std::io { .. }：导入该模块全部当前可见符号
// 4) ".." 必须单独出现，不能与其他导入项混用
// 5) 模块名来自 package manifest（koral.json / std/koral.json），编译器不再从源码语法推断子模块树
// 6) 顶层 manifest entry 表示默认目标模块名，不是源码文件路径

// ============================================================================
// Global Declarations
// ======================ref======================================================

<global-decl> ::= <access-modifier>? "intrinsic"? <let-decl>
               | <access-modifier>? "foreign" <foreign-let-decl>
               | <access-modifier>? "intrinsic"? <type-decl>
               | <access-modifier>? "foreign" <foreign-type-decl>
               | <access-modifier>? <trait-decl>
               | "intrinsic"? <given-decl>

<access-modifier> ::= "public"
                    | "private"
                    | "protected"
                    | "protected" "public"

// ============================================================================
// Let Declarations (Variables and Functions)
// ============================================================================

<let-decl> ::= "let" <identifier> <generic-params>? <function-signature> "=" <expression> ";"?
             | "let" "mutable"? <identifier> <type-annotation>? "=" <expression> ";"?
             | "let" <identifier> <generic-params>? <function-signature> ";"?  // intrinsic function
// 说明：出现 mutable 时必须是变量声明（不可接函数参数列表）

<foreign-let-decl> ::= "let" "mutable"? <identifier> <type-annotation> ";"?
                     | "let" <identifier> "(" <func-param-list>? ")" <type-annotation> ";"?
// 说明：
// 1) foreign 变量声明必须有类型（支持 mutable）
// 2) foreign 函数不允许函数体，不支持泛型

<function-signature> ::= "(" <func-param-list>? ")" <type-annotation>
<func-param-list> ::= <func-param> ("," <func-param>)* ","?
<func-param> ::= "mutable"? <identifier> ":" <type-annotation>    // named parameter
               | "mutable"? <identifier> <type-annotation>          // positional parameter
// Named parameters use `name: Type` syntax. Callers must provide the label at the call site.
// Positional parameters use `name Type` syntax (no colon). Each parameter independently
// decides whether it is named or positional; mixing is allowed in the same parameter list.
// `self` parameters and `foreign` declarations do not support named parameter syntax.

<method-param-list> ::= <method-first-param> ("," <func-param>)* ","?
<method-first-param> ::= "self"                                          // bare self
                       | "*self"                                          // managed receiver sugar
                       | "*mutable self"                                      // mutable managed receiver sugar
                       | <func-param>
// Receiver rules:
// - *self / *mutable self: managed receiver sugar, auto-ref allowed
// - bare self: equivalent to managed value receiver
// - auto-deref (*T -> T) only applies to self receivers (following Go)

<type-annotation> ::= <type>

// ============================================================================
// Type Declarations
// ============================================================================

<type-decl> ::= "type" <type-identifier> <generic-params>? <type-body>
              | "type" <type-identifier> <generic-params>?            // intrinsic type (no body)
              | "type" <type-identifier> "=" <type>                   // type alias
// 说明：类型别名不支持泛型参数，但目标类型可以是泛型实例化类型（如 List[Int]）
// 类型别名在语义检查阶段被完全消除，后续阶段无需感知其存在
<type-body> ::= "(" <field-list>? ")"                 // struct
              | "{" <variant-list>? "}"                // enum (sum type)

<foreign-type-decl> ::= "type" <string-literal>? <type-identifier> <foreign-type-body> ";"?
<foreign-type-body> ::= "(" <field-list>? ")"    // FFI struct with fields
                      | "{" "}"                   // opaque type (empty enum, no variants)
// 说明：
// 1) 可选 string-literal 为 C 侧类型名（cname）
// 2) foreign type 可以有字段（用于 FFI 结构体布局对齐），也可以用空大括号表示不透明类型
// 3) 空大括号 `{}` 表示没有变体的 enum，与普通 enum 语法一致

<field-list> ::= <field> ("," <field>)* ","?
<field> ::= <access-modifier>? "mutable"? <identifier> ":" <type-annotation>    // named field
          | <access-modifier>? "mutable"? <identifier> <type-annotation>        // positional field

<variant-list> ::= <variant> ("," <variant>)* ","?
<variant> ::= <type-identifier> "(" <variant-field-list>? ")"
<variant-field-list> ::= <variant-field> ("," <variant-field>)* ","?
<variant-field> ::= <identifier> ":" <type-annotation>    // named field
                  | <identifier> <type-annotation>          // positional field

// ============================================================================
// Trait Declarations
// ============================================================================

<trait-decl> ::= "trait" <type-identifier> <generic-params>? <trait-parents>? "{" <trait-member>* "}"
<trait-parents> ::= <type> ("and" <type>)*
<trait-member> ::= <access-modifier>? <identifier> <generic-params>? "(" <method-param-list>? ")" <type-annotation> ";"?
// 说明：trait method 支持泛型参数，但不支持方法体

// ============================================================================
// Given Declarations (Type Implementations)
// ============================================================================

<given-decl> ::= "given" <generic-params>? <type> ("as" <type>)? "{" <given-member>* "}"
<given-member> ::= <access-modifier>? <identifier> <generic-params>? "(" <method-param-list>? ")" <type-annotation> "=" <expression> ";"?
                 | <access-modifier>? <identifier> <generic-params>? "(" <method-param-list>? ")" <type-annotation> ";"?  // intrinsic given method
// 说明：
// 1) `given Type { ... }` 为类型固有实现
// 2) `given Type as Trait { ... }` 为 trait 实现

// ============================================================================
// Generic Parameters
// ============================================================================

<generic-params> ::= "[" <generic-param-list> "]"
<generic-param-list> ::= <generic-param> ("," <generic-param>)* ","?
<generic-param> ::= <type-identifier> <trait-constraints>
<trait-constraints> ::= <trait-constraint> ("and" <trait-constraint>)*
<trait-constraint> ::= <type-identifier>
                     | <generic-type>
// 说明：当前 parser 约束里支持两类：
// 1) 简单 trait 名（如 Any, Equatable）
// 2) 泛型 trait（如 Iterator[T]）

// ============================================================================
// Types
// ============================================================================

<type> ::= <type-prefix>* <type-atom>

<type-atom> ::= <type-identifier>
              | <generic-type>
              | <function-type>
              | "Self"

<type-prefix> ::= "*" "mutable"?
               | "?*" "mutable"?
               | "unsafe" "*" "mutable"?
// *T             — managed read-only reference
// *mutable T         — managed mutable reference
// unsafe * T         — read-only pointer
// unsafe * mutable T     — mutable pointer
// ?*T            — read-only weak reference
// ?*mutable T        — mutable weak reference
// Note: `mutable` must follow the keyword, not precede it (e.g. `*mutable T`, not `mutable *T`).

<generic-type> ::= <type-identifier> "[" <type-list> "]"
<function-type> ::= "Func" "(" <function-type-param-list>? ")" <type>
<function-type-param-list> ::= <type> ("," <type>)* ","?
// Function type: Func(ParamType1, ParamType2, ...) ReturnType
// The types inside parentheses are parameter types, followed by a return type
// Examples:
//   Func() Int           - () -> Int (no params, returns Int)
//   Func(Int) Int        - (Int) -> Int (one param, returns Int)
//   Func(Int, Int) Int - (Int, Int) -> Int (two params, returns Int)
// Module-prefixed type syntax is not part of the language. Import concrete
// type symbols with `using module::path { TypeName }`, then refer to `TypeName`
// directly.

<type-list> ::= <type> ("," <type>)* ","?

// ============================================================================
// Statements
// ============================================================================

<statement> ::= <let-statement>
              | <assignment-statement>
              | <if-statement>
              | <while-statement>
              | <for-statement>
              | <when-statement>
              | <return-statement>
              | <break-statement>
              | <yield-statement>
              | <continue-statement>
              | <defer-statement>
              | <expression-statement>

<let-statement> ::= "let" "mutable"? <identifier> <type-annotation>? "=" <expression> ";"?
                | "let" "(" <pair-binding> "," <pair-binding> ")" "=" <expression> ";"?

<pair-binding> ::= "_"
                | "mutable"? <identifier> <type-annotation>?

<assignment-statement> ::= <expression> <assign-op> <expression> ";"?
<assign-op> ::= "=" | "+=" | "-=" | "*=" | "/=" | "%=" | "&=" | "|=" | "^=" | "<<=" | ">>="

<return-statement> ::= "return" <expression>? ";"?
<break-statement> ::= "break" ";"?
<yield-statement> ::= "yield" <expression> ";"?
<continue-statement> ::= "continue" ";"?

<defer-statement> ::= "defer" <expression> ";"?
// Semantic restriction: `yield <expression>` is only valid inside the body of
// an `if` or `when` expression branch. It produces the value of the enclosing
// branch expression and supports early exit from that branch body.
// Plain `break` (without expression) exits the nearest enclosing `while`/`for` loop.

<if-statement> ::= "if" <expression> "then" <expression> ("else" <expression>)?
<while-statement> ::= "while" <expression> "then" <expression>
<for-statement> ::= "for" <match-pattern> "in" <expression> "then" <expression>
<when-statement> ::= "when" <expression> "in" "{" <statement-match-arm-list> "}"
<statement-match-arm-list> ::= <statement-match-arm> ("," <statement-match-arm>)* ","?
<statement-match-arm> ::= <match-pattern> "then" <expression>

<expression-statement> ::= <expression> ";"?

// ============================================================================
// Expressions
// ============================================================================

<expression> ::= <if-expression>
               | <when-expression>
               | <or-return-expression>

<if-expression> ::= "if" <expression> "then" <expression> ("else" <expression>)?

<when-expression> ::= "when" <expression> "in" "{" <match-arm-list> "}"
<match-arm-list> ::= <match-arm> ("," <match-arm>)* ","?
<match-arm> ::= <match-pattern> "then" <expression>
// `if` / `when` are expressions.
// `while` / `for` use the same surface syntax shape but are statement-only.

// ============================================================================
// Binary Expressions (by precedence, lowest to highest)
// ============================================================================

<or-return-expression> ::= <or-else-expression> ("or" "return")*
// "or return" is early-return propagation for Option/Result types

<or-else-expression> ::= <or-expression> ("or" "else" <or-expression>)*
// "or else" is value coalescing for Option/Result types

<or-expression> ::= <and-then-expression> ("or" <and-then-expression>)*

<and-then-expression> ::= <and-expression> ("and" "then" <and-expression>)*
// "and then" is optional chaining / value transformation for Option/Result types

<and-expression> ::= <not-expression> ("and" <not-expression>)*
<not-expression> ::= "not" <is-expression> | <is-expression>

<is-expression> ::= <bitwise-or-expression>
                  | <bitwise-or-expression> "is" <pattern>
                  | <bitwise-or-expression> "is" "not" <pattern>

<bitwise-or-expression> ::= <bitwise-xor-expression> ("|" <bitwise-xor-expression>)*
<bitwise-xor-expression> ::= <bitwise-and-expression> ("^" <bitwise-and-expression>)*
<bitwise-and-expression> ::= <range-expression> ("&" <range-expression>)*

<range-expression> ::= <comparison-expression> <range-suffix>?
                     | <range-prefix>
                     | <range-prefix> <comparison-expression>

<range-suffix> ::= <range-op> <comparison-expression>
                 | <postfix-range-op>

<range-op> ::= ".." | "..<" | "<.." | "<..<"
<postfix-range-op> ::= ".." | "<.."
<range-prefix> ::= ".." | "..<"

<comparison-expression> ::= <shift-expression> <comparison-tail>?

<comparison-tail> ::= <equality-op> <shift-expression>
                    | <ascending-chain-tail>
                    | <descending-chain-tail>

<equality-op> ::= "==" | "<>"

<ascending-chain-tail> ::= <ascending-op> <shift-expression>
                           (<ascending-op> <shift-expression>)+

<descending-chain-tail> ::= <descending-op> <shift-expression>
                            (<descending-op> <shift-expression>)+

<ascending-op> ::= "<" | "<="
<descending-op> ::= ">" | ">="

<shift-expression> ::= <additive-expression> (<shift-op> <additive-expression>)*
<shift-op> ::= "<<" | ">>"

<additive-expression> ::= <multiplicative-expression> (<additive-op> <multiplicative-expression>)*
<additive-op> ::= "+" | "-"

<multiplicative-expression> ::= <prefix-expression> (<multiplicative-op> <prefix-expression>)*
<multiplicative-op> ::= "*" | "/" | "%"

// ============================================================================
// Prefix Expressions
// ============================================================================

<prefix-expression> ::= "-" <prefix-expression>
                      | "~" <prefix-expression>
                      | "*" <prefix-expression>
                      | "unsafe" "&" "mutable"? <prefix-expression>
                      | "&" "mutable"? <prefix-expression>
                      | <postfix-expression>
// Prefix ref/pointer operators:
// - *expr dereferences managed references and raw pointers
// - &expr / &mutable expr form managed references
// - unsafe & expr / unsafe & mutable expr form raw pointers
// Mutability/addressability checks are semantic, not grammar-level.

// ============================================================================
// Postfix Expressions
// ============================================================================

<postfix-expression> ::= <primary-expression> <postfix-suffix>*
<postfix-suffix> ::= <call-suffix>
                   | <subscript-suffix>
                   | <member-suffix>
                   | <cast-suffix>
                   | <qualified-method-suffix>
                   | <generic-method-suffix>

<call-suffix> ::= "(" <argument-list>? ")"
<subscript-suffix> ::= "[" <expression-list> "]"
<member-suffix> ::= "." <identifier>
<cast-suffix> ::= "(" <type> ")"

// 限定方法调用：消歧义，指定 trait。可选地附带 trait 实参与方法级泛型参数。
// 例: (n as Tag).plus_ten()  /  (b as Transform[Int]).apply[Out](f)
<qualified-method-suffix> ::= "." <identifier> ("[" <type-list> "]")? "(" <argument-list>? ")"
<qualified-base-expression> ::= "(" <expression> "as" <type> ")"

// 泛型方法调用：方法级泛型参数，不指定 trait。
// 例: b.transform[Int](f)
<generic-method-suffix> ::= "." <identifier> "[" <type-list> "]" "(" <argument-list>? ")"

<argument> ::= <identifier> ":" <expression>    // named argument
             | <expression>                      // positional argument
<argument-list> ::= <argument> ("," <argument>)* ","?
<expression-list> ::= <expression> ("," <expression>)* ","?

// ============================================================================
// Primary Expressions
// ============================================================================

<primary-expression> ::= <literal>
                       | <identifier>
                       | "self"
                       | "it"
                       | <implicit-member-expression>
                       | <collection-literal>
                       | <block-expression>
                       | <lambda-expression>
                       | <pair-literal>
                       | "(" <expression> ")"

<pair-literal> ::= "(" <expression> "," <expression> ")"

// Generic instantiation / explicit generic call is postfix:
//   Box[Int](42)
//   make[Int](1)

<collection-literal> ::= "[" "]"
                       | "[" <expression-list> ","? "]"
                       | "[" <map-entry-list> ","? "]"

<map-entry-list> ::= <map-entry> ("," <map-entry>)*
<map-entry> ::= <expression> ":" <expression>

<implicit-member-expression> ::= "." (<type-identifier> | <identifier>) "(" <argument-list>? ")"
// 隐式成员表达式：当期望类型已知时，可以省略类型名直接使用 .成员名
// 用于 enum case 构造和静态方法调用
// 例如: .Some(42), .None(), .new(), .with_capacity(10)

<block-expression> ::= "{" <statement>* "}"

<lambda-expression> ::= "(" <lambda-param-list>? ")" <type-annotation>? "->" <expression>
<lambda-param-list> ::= <lambda-param> ("," <lambda-param>)* ","?
<lambda-param> ::= <identifier> <type-annotation>?
// Lambda expression examples:
//   () -> 42                           - no params
//   (x) -> x * 2                       - single param, type inferred
//   (x Int) -> x * 2                   - single param with type
//   (x, y) -> x + y                    - multiple params, types inferred
//   (x Int, y Int) Int -> x + y        - full type annotations
//   (x) -> { let y = x * 2; return y + 1 } - block body

// ============================================================================
// Patterns
// ============================================================================

<pattern> ::= <primary-pattern>
           | <comparison-pattern>
// Used by `is` expressions. Logical pattern combinators are not allowed here.
// Comparison patterns (e.g. `>= 60`, `> 10`) are allowed in `is` expressions.

<match-pattern> ::= <or-pattern>
// Used only by `when ... in` arms. Logical pattern combinators remain supported here.

<or-pattern> ::= <and-pattern> ("or" <and-pattern>)*
<and-pattern> ::= <not-pattern> ("and" <not-pattern>)*
<not-pattern> ::= "not" <not-pattern>
               | <primary-pattern>

<primary-pattern> ::= "_"                                     // wildcard
                    | "-"? <integer-literal>                  // integer literal pattern (including negative)
                    | <bool-literal>                          // bool literal pattern
                    | <string-literal>                        // string literal pattern
                    | <rune-literal>                          // rune literal pattern
                    | "mutable"? <identifier>                     // variable binding
                    | <struct-pattern>                        // struct destructuring pattern
                    | <pair-pattern>                          // pair destructuring pattern
                    | <comparison-pattern>                    // comparison pattern
                    | <enum-case-pattern>                    // enum case pattern
                    | "(" <pattern> ")"                       // parenthesized pattern (grouping)

<pair-pattern> ::= "(" <pattern> "," <pattern> ")"

<struct-pattern> ::= <type-identifier> "(" <pattern-arg-list>? ")"
// Struct destructuring pattern: matches a struct value and destructures its fields by position.
// TypeName must refer to a struct type. The number of sub-patterns must match the number of fields.
// Sub-patterns can be any pattern (wildcard, variable binding, literal, nested struct, etc.)
// Examples:
//   Point(x, y)              - bind fields to x and y
//   Point(_, y)              - ignore first field, bind second
//   Point(0, y)              - match first field literally, bind second
//   Rect(Point(a, b), w, h)  - nested struct destructuring
//   Box(v)                   - works with generic structs (e.g., Box[Int])

<comparison-pattern> ::= <comparison-pattern-op> "-"? <integer-literal>
<comparison-pattern-op> ::= ">" | "<" | ">=" | "<="
// Comparison patterns: > n, < n, >= n, <= n (where n is an integer literal, optionally negative)

<enum-case-pattern> ::= "." <type-identifier> ("(" <pattern-arg-list>? ")")?
<pattern-arg> ::= <identifier> ":" <pattern>    // named pattern argument
                | <pattern>                      // positional pattern argument
<pattern-arg-list> ::= <pattern-arg> ("," <pattern-arg>)* ","?

// ============================================================================
// Literals
// ============================================================================

<literal> ::= <integer-literal>
            | <float-literal>
            | <duration-literal>
            | <string-literal>
            | <rune-literal>
            | <interpolated-string-literal>
            | <bool-literal>

<duration-literal> ::= <decimal-literal> <duration-suffix>
<duration-suffix> ::= "s" | "ms" | "us" | "ns"

<integer-literal> ::= <decimal-literal>
                  | <binary-literal>
                  | <octal-literal>
                  | <hex-literal>

<decimal-literal> ::= [0-9]+ ("_" [0-9]+)*
<binary-literal> ::= "0b" [0-1]+ ("_" [0-1]+)*
<octal-literal> ::= "0o" [0-7]+ ("_" [0-7]+)*
<hex-literal> ::= "0x" [0-9a-fA-F]+ ("_" [0-9a-fA-F]+)*

<float-literal> ::= <decimal-digits> "." <decimal-digits>
                 | <decimal-digits> "e" ("+" | "-")? <decimal-digits>
                 | <decimal-digits> "." <decimal-digits> "e" ("+" | "-")? <decimal-digits>

<decimal-digits> ::= [0-9]+ ("_" [0-9]+)*

<string-literal> ::= '"' <string-char>* '"'
                   | '"""' "\n" <multiline-content> '"""'

<rune-literal> ::= "'" <rune-char>* "'"

<multiline-string-literal> ::= '"""' "\n" <multiline-content> <multiline-closing>
<multiline-content> ::= (<multiline-line> "\n")*
<multiline-line> ::= <any character except newline>*
<multiline-closing> ::= <indent-whitespace> '"""'
// The leading whitespace of the closing """ defines the indentation prefix
// that is stripped from every content line.
// Every non-empty content line must start with at least that prefix.

<interpolated-string-literal> ::= '"' (<string-char> | <interpolation>)* '"'
                                | '"""' "\n" (<multiline-char> | <interpolation>)* <multiline-closing>
<interpolation> ::= "\(" <expression> ")"

<string-char> ::= <escape-sequence> | ~["\\\n]
<rune-char> ::= <escape-sequence> | ~['\\\n]
<escape-sequence> ::= "\\" ("n" | "t" | "r" | "v" | "f" | "0" | "\\" | '"' | "'")
                    | "\\x" [0-9a-fA-F] [0-9a-fA-F]
                    | "\\u{" [0-9a-fA-F]+ "}"
// \xHH  — exactly 2 hex digits, produces a single byte (0x00–0xFF)
// \u{H…} — 1 to 6 hex digits, produces a Unicode scalar value (must be a valid Unicode scalar)

<bool-literal> ::= "true" | "false"

<identifier> ::= "_"* [a-z] [a-zA-Z0-9_]*
             | "_"+
// 说明：identifier 以小写 ASCII 字母开头，允许 _ 前缀；单独的下划线序列也合法（用于占位）

<type-identifier> ::= "_"* [A-Z] [a-zA-Z0-9_]*
// 说明：type-identifier 以大写 ASCII 字母开头，允许 _ 前缀

// ============================================================================
// Keywords
// ============================================================================

// Reserved keywords:
//   let, mutable, type, trait, given, intrinsic, foreign
//   using, self, Self, as, unsafe
//   if, then, else, while, for, when, is, in, it
//   and, or, not
//   return, break, continue, defer
//   true, false
//   public, private, protected, protected public

// ============================================================================
// Operators
// ============================================================================

// Arithmetic: + - * / %
// Comparison: == <> > < >= <=
// Logical: and or not
// Bitwise: & | ^ ~ << >>
// Assignment: = += -= *= /= %= &= |= ^= <<= >>=
// Range: .. ..< <.. <..<
// Lambda: ->
// Early-return propagation: or return
// Value coalescing: or else
// Optional chaining: and then
// Other: . , ; : ( ) [ ] { }

// ============================================================================
// Implicit Conversion Rules (semantic, not grammar)
// ============================================================================

// No implicit ref promotion (T → *T) for function/method arguments.
// No auto-deref (*T → T) for function/method arguments.
// Use explicit &, &mutable, unsafe &, unsafe &mutable, and * at call sites.
// Auto-ref and auto-deref only apply to method receivers (self).
// - *self: auto-ref allowed (T → *T)
// - *mutable self: auto-ref allowed when the receiver is a writable lvalue (T → *mutable T)
// - self: auto-deref allowed (*T → T, following Go's pointer receiver behavior)

// Closures: let mutable variables are captured by reference (byMutReference).
// Mutations inside the closure are visible outside.

// ============================================================================
// Comments
// ============================================================================

// Line comment: // ...
// Block comment: /* ... */

// ============================================================================
// Automatic Semicolon Insertion
// ============================================================================

// Koral uses newline-based statement termination with continuation rules:
// - Semicolons are optional at end of statements/declarations
// - A newline terminates a statement by default
// - A newline may continue only when:
//   1. The next token is a grammar join token (`and`, `or`, `is`, `then`, `else`, including `and then`, `or else`, `or return`)
//   2. The next token is `.` or `->`
//   3. The parser is still inside unclosed `()`, `[]`, or `{}` grouping
// - Blank lines and comments do not change this by themselves
// - Ordinary operators such as `+`, `*`, `==`, `<<`, and `..` do not join across newlines
