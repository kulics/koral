# Project Rules

## Git
- **Never commit for the user.** All git commits must be done by the user themselves. Do not run `git commit`, `git add`, or `git push` unless explicitly asked.
- Stashing (`git stash`) is allowed when needed for tooling.
