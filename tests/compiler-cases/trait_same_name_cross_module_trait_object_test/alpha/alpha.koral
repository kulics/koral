public trait Tag {
    value(*self) Int
}

type A(v Int)

given A as Tag {
    value(*self) Int = self.v
}

public let read_tag(r * Tag) Int = r.value()

public let run_alpha() Int = {
    let a = A(7)
    let r * Tag = &a
    return read_tag(r)
}