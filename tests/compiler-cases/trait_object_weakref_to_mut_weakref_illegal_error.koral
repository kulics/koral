// EXPECT-ERROR: Type mismatch: expected ?*mut CounterOps, got ?*CounterOps

trait CounterOps {
    peek(*self) Int
}

type Counter(value Int)

given Counter as CounterOps {

    public peek(*self) Int = self.value
}

let main() Int = {
    let strong *mut CounterOps = box(Counter(3))
    let readonly * CounterOps = strong
    let weak ?* CounterOps = downgrade(readonly)
    let _ ?*mut CounterOps = weak
    return 0
}
