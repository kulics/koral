// EXPECT-ERROR: Type mismatch: expected weakref mut CounterOps, got weakref CounterOps

trait CounterOps {
    peek(self ref) Int
}

type Counter(value Int)

given Counter as CounterOps {

    public peek(self ref) Int = self.value
}

let main() Int = {
    let strong ref mut CounterOps = box(Counter(3))
    let readonly ref CounterOps = strong
    let weak weakref CounterOps = readonly.weakref
    let _ weakref mut CounterOps = weak
    return 0
}