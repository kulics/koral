// EXPECT-ERROR: Cannot take '&mutable' from immutable binding 'vec'

type Vec(mutable x Int)
type Nest(mutable vec Vec)

let main() Int = {
    let n = Nest(Vec(1))
    let _ *mutable Vec = &mutable n.vec
    return 0
}
