// EXPECT-ERROR: Cannot take '&mut' from immutable binding 'vec'

type Vec(mut x Int)
type Nest(mut vec Vec)

let main() Int = {
    let n = Nest(Vec(1))
    let _ *mut Vec = &mut n.vec
    return 0
}
