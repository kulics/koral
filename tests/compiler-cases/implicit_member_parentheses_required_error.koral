// EXPECT-ERROR: Unexpected token: ;, expected: '(' after implicit member name

type Flag {
    On(),
    Off(),
};

type Counter(value Int);

given Counter {
    new() Self = Counter(0);
};

let make_flag() Flag = .On;

let make_counter() Counter = .new;

let main() Void = {
    let _ = make_flag();
    let _ = make_counter();
};