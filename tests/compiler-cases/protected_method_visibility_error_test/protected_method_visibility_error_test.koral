// EXPECT-ERROR: Cannot directly construct type 'SecretBox' because field 'value' is protected. Use a public factory method instead.

using protected_method_visibility_error_test::models { SecretBox }

public let main() Int = {
    let box = SecretBox(21)
    println(box.value.to_string())
    return 0
}