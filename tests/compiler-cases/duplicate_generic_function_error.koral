// EXPECT-ERROR: Duplicate definition: wrap

// Generic free functions must be duplicate-checked the same way as
// non-generic ones.

let wrap[T Any](value T) T = value;

let wrap[T Any](value T) T = value;

let main() Int = {
    return wrap(0);
};
