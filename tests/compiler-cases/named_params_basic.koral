// EXPECT: OK
// EXPECT: 150

type Button(width Int, height Int, label String)

let main() Int = {
    let b = Button(label: "OK", height: 50, width: 3)
    if b is Button(label: text, width: w, height: h) then {
        println(text)
        println(w * h)
    }
    return 0
}
