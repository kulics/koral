// EXPECT: 3

type Counter(mut value Int)

given Counter {
    public keep(self ref Self) ref Self = self
}

let main() Int = {
    let owned ref mut Counter = box(Counter(3))
    let readonly ref Counter = owned.keep()
    println(readonly.val.value.to_string())
    return 0
}
