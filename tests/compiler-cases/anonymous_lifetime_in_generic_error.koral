// EXPECT-ERROR: Lifetime parameters are not supported in generic parameter lists

// Negative test: '_ in generic parameter lists should also be rejected.

let foo['_](x ref '_ Int) Int = x.val

let main() Int = {
    return 0
}
