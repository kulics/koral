// EXPECT: boom

let make(opt Option[String]) Result[String] = when opt in {
    .Some(e) then .Error(box(e)),
    .None then .Ok("ok"),
}

let main() Void = {
    let r = make(.Some("boom"))
    when r in {
        .Error(e) then println(e.message()),
        .Ok(v) then println(v),
    }
}
