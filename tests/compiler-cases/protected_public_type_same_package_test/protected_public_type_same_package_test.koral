// EXPECT: 42

using protected_public_type_same_package_test::models { PackageToken }

protected public let reveal(token PackageToken) Int = token.value

public let main() Int = {
    println(reveal(PackageToken(42)).to_string())
    return 0
}