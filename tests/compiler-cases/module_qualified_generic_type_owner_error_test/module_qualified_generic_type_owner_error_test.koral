// EXPECT-ERROR: file merge syntax no longer supports aliases

using "alpha" as Alpha

type LocalBox[T Any](value T)

let main() Int = {
    let value Alpha.LocalBox[Int] = LocalBox[Int](1)
    return value.value
}
