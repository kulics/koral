// EXPECT-ERROR: spawn_thread: first argument must be unsafe * mutable unsafe * UInt8

let main() Int = {
    let wrong unsafe * mutable UInt8 = null_ptr()
    let out_tid unsafe * mutable UInt64 = null_ptr()
    spawn_thread(wrong, out_tid, () -> {}, 0(UInt64))
    return 0
}