// EXPECT: 2
// EXPECT: alias generic static receiver ok

type IntList = List[Int]

let main() Int = {
    let mutable xs IntList = IntList.new()
    xs.push(1)
    xs.push(2)
    println(xs.count())
    println("alias generic static receiver ok")
    return 0
}