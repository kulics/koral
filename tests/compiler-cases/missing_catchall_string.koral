// Test missing catchall pattern for String type
// EXPECT-ERROR: requires a wildcard or variable binding

let main() Int = {
    let s = "hello"
    
    // Error: String has infinite domain, needs catchall
    let res = when s in {
        "hello" then 1,
        "world" then 2,
    }
    
    yield 0
}
