// Test case for indirect recursion through generic type
// EXPECT-ERROR: Indirect recursion detected

// Generic wrapper type
type [T Any]Wrapper(inner T)

// Foo contains Wrapper[Foo], which expands to contain Foo
// This creates an infinite-size type
type Foo(w [Foo]Wrapper)

let main() Int = 0
