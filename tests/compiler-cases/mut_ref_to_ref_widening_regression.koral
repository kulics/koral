// EXPECT: ok

type Boxed(value Int)

let read_value(x Boxed ref) Int = x.value

let main() Void = {
    let mr Boxed mut ref = box(Boxed(7))
    let r Boxed ref = mr
    assert(read_value(r) == 7, "mut ref should widen to ref")
    println("ok")
}
