// EXPECT: ok

type Boxed(value Int)

let read_value(x ref Boxed) Int = x.value

let main() Void = {
    let mr ref mut Boxed = box(Boxed(7))
    let r ref Boxed = mr
    assert(read_value(r) == 7, "ref mut should widen to ref")
    println("ok")
}
