// EXPECT: ok

type Boxed(value Int)

let read_value(x * Boxed) Int = x.value

let main() Void = {
    let mr *mut Boxed = box(Boxed(7))
    let r * Boxed = mr
    assert(read_value(r) == 7, "*mut should widen to *")
    println("ok")
}
