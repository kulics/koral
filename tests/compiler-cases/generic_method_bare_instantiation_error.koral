// EXPECT-ERROR: after generic method name

type Box(value Int);

given Box {
    public map[T Any](*self, f Func(Int) T) T = f(self.value);
};

let main() Int = {
    let box = Box(1);
    let _ = box.map[Int];
    return 0;
};