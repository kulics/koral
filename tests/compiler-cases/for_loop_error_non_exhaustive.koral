// Test error: non-exhaustive pattern in for loop

// EXPECT-ERROR: exhaustive

let main() Void = {
    let mut list = List[Int].new()
    list.push(1)
    
    // Literal pattern is not exhaustive
    for 42 in list then {
        println("matched")
    }
}
