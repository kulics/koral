// EXPECT: 99
// EXPECT: 7

using package_private_method_same_package_test::models { LocalBox };

public let main() Int = {
    println(LocalBox.answer().to_string());
    let box = LocalBox.new(7);
    println(box.reveal().to_string());
    return 0;
};
