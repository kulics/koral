// EXPECT-ERROR: Type mismatch: expected *unsafe mutable Int, got *unsafe Int

let main() Int = {
    let raw_ptr *unsafe Int = null_ptr()
    let owner *mutable Int = box(0)
    let _ = make_mut_ref[Int, Int](raw_ptr, owner)
    return 0
}