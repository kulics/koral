// EXPECT-ERROR: Type mismatch: expected Int *! mutable, got *! Int

let main() Int = {
    let raw_ptr *! Int = null_ptr()
    let owner *mutable Int = box(0)
    let _ = make_mut_ref[Int, Int](raw_ptr, owner)
    return 0
}