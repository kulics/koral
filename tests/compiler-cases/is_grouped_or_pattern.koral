// EXPECT: grouped_or_pattern: PASS

let main() Int = {
    let x = 1;
    if x is (0 or 1) then {
        println("grouped_or_pattern: PASS");
        return 0;
    };
    println("grouped_or_pattern: FAIL");
    return 1;
};