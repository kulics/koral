// Test that duplicate match arms are detected
// EXPECT-ERROR: Duplicate pattern

let main() Void = {
    let x = true;
    let result = when x in {
        true then "yes",
        false then "no",
        true then "duplicate yes",
    };
    println(result);
};