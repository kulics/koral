// EXPECT: true
// EXPECT: false

trait [T Any] MyEq {
    equals(self, other T) Bool
}

given [T Any] [T]MyEq {
    neq(self, other T) Bool = not self.equals(other)
}

type [T Eq] Box(value T)

given [T Eq] [T]Box [[T]Box]MyEq {
    equals(self, other [T]Box) Bool = self.value == other.value
}

let main() Int = {
    let a = [Int]Box(1)
    let b = [Int]Box(2)
    println(a.(MyEq)neq(b))
    println(a.neq(a))
    yield 0
}
