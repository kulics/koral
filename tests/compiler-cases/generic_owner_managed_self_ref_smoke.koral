// EXPECT-EXACT: 7

type Box[T Any](mut value T)

given[T Any] Box[T] {
    public keep(self ref Self) ref Self = self
}

let main() Int = {
    let strong ref mut Box[Int] = box(Box[Int](7))
    let alias ref Box[Int] = strong.keep()
    println(alias.val.value.to_string())
    return 0
}