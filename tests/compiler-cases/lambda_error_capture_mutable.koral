// Lambda capture mutable variable test
// Closures can now capture 'let mutable' variables by reference.

let main() Int = {
    let mutable x = 10
    let f Func() Int = () -> x + 1
    println("PASS")
    return 0
}
