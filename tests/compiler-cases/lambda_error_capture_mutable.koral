// Lambda capture mutable variable test
// Closures can now capture 'let mut' variables by reference.

let main() Int = {
    let mut x = 10
    let f Func[Int] = () -> x + 1
    println("PASS")
    return 0
}
