// EXPECT-ERROR: Cannot capture mutable variable 'x'

// Mutable parameters are mutable bindings too and cannot be captured.

let apply(mutable x Int) Func() Int = {
    return () -> x + 1;
};

let main() Int = {
    let f = apply(10);
    println(f().to_string());
    return 0;
};
