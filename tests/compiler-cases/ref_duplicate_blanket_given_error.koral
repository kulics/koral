// Tests that blanket given for T ref outside std/trait module is rejected by orphan rule
// Feature: type-modifier-trait-conformance
// EXPECT-ERROR: Cannot declare

given[T Eq and Deref] T ref Eq {
    public equals(self, other T ref) Bool = self.val.equals(other.val)
}

let main() Void = {
    println("should not compile")
}
