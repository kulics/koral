// EXPECT: true
// EXPECT: true
// EXPECT: 42

type Holder(mut storage ref Int)

type Outer(mut inner Holder)

let main() Int = {
    let outer = box(Outer(Holder(box(42))))

    println(is_unique_mutable(outer.inner.storage))
    println(is_unique_mutable(outer.inner.storage))
    println(outer.inner.storage.val)

    return 0
}