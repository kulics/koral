// EXPECT: true
// EXPECT: true
// EXPECT: 42

type Holder(mutable storage * Int)

type Outer(mutable inner Holder)

let main() Int = {
    let outer = box(Outer(Holder(box(42))))

    println(is_unique_mutable(unsafe & outer.inner.storage))
    println(is_unique_mutable(unsafe & outer.inner.storage))
    println(*outer.inner.storage)

    return 0
}