let main() Void = {
    let p = alloc_memory[Int](1)
    init_memory(p, 42)

    let u UInt = p(UInt)
    let p2 *unsafe Int = u(*unsafe Int)

    println(*p2)
    dealloc_memory(p2)
}

// EXPECT: 42
