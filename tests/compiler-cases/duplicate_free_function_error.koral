// EXPECT-ERROR: Duplicate definition: compute

// Global free functions do not overload: two definitions with the same
// name must be rejected instead of silently shadowing each other.

let compute(x Int) Int = x + 1;

let compute(x Int) Int = x + 2;

let main() Int = {
    return compute(0);
};
