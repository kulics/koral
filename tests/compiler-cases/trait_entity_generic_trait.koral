// EXPECT: true
// EXPECT: false

trait [T Any] MyEq {
    equals(self, other T) Bool
}

given [T Any] [T]MyEq {
    neq(self, other T) Bool = not self.equals(other)
}

type Num(x Int)

given Num [Num]MyEq {
    equals(self, other Num) Bool = self.x == other.x
}

let main() Int = {
    let a = Num(1)
    let b = Num(2)
    println(a.(MyEq)neq(b))
    println(a.neq(a))
    yield 0
}
