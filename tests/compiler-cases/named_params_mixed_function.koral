// EXPECT: hello8080

let connect(host String, port: Int = 80) String = {
    let mutable s = StringBuilder.new();
    s.push_string(host);
    s.push_string(port.to_string());
    return s.to_string();
};

let main() Int = {
    println(connect("hello", port: 8080));
    return 0;
};
