// Test that from_seed(0, 0, 0, 0) triggers panic
// Validates: Requirements 3.3

// EXPECT-ERROR: Panic: DefaultRandomSource: seed must not be all zeros

using Std.Rand.*

let main() Void = {
    let rng = DefaultRandomSource.from_seed(0, 0, 0, 0)
    println(rng.generate())
}
