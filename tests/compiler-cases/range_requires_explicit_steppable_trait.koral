// EXPECT-ERROR: Type Num does not explicitly implement trait Step

type Num()

given Num as Eq {

    equals(self, other Num) Bool = true
}

given Num as Ord {

    compare(self, other Num) Int = 0
}

given Num {

    max_value() Self = Num()
    min_value() Self = Num()
    succ(self) Option[Self] = Option[Self].Some(self)
    pred(self) Option[Self] = Option[Self].Some(self)
}

let main() Int = {
    let r = Num()..Num()
    for _ in r then {}
    yield 0
}
