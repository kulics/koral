// EXPECT-ERROR: Unknown named argument 'wrong' for 'make'

// Labels on static method calls must be validated, not silently dropped.

type Factory(limit: Int);

given Factory {
    public make(limit: Int) Factory = Factory(limit: limit);
};

let main() Int = {
    let f = Factory.make(wrong: 3);
    return f.limit;
};
