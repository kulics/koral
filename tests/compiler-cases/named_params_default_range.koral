// EXPECT: Full

type Span(range: Range[Int] = ..);

let main() Int = {
    let s = Span();
    when s.range in {
        .Full() then println("Full"),
        _ then println("Other"),
    };
    return 0;
};
