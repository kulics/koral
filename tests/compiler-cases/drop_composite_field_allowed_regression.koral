// EXPECT: outer dropped

type Inner(value Int)

type Outer(inner Inner, name String)

given Outer as Drop {
    drop(source mut ptr Self) Void = {
        println("outer dropped")
    }
}

let main() Int = {
    let _ = Outer(Inner(1), "resource")
    return 0
}
