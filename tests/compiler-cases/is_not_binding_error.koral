// EXPECT-ERROR: Variable bindings are not allowed in 'is not' expression

type MyOption {
    None(),
    Some(value Int),
}

let main() Int = {
    let opt = MyOption.Some(1)
    if opt is not .Some(value) then {
        println("unexpected")
    }
    yield 0
}