// EXPECT-ERROR: Cannot access module_private method 'reveal'

using module_private_method_access_error_test::models { SecretBox };

public let main() Int = {
    let box = SecretBox.new(21);
    return box.reveal();
};
