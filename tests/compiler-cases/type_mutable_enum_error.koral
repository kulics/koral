// EXPECT-ERROR: Enum type cannot be marked mutable

// Declaration-site mutability is only valid on nominal types.
// Enums must not accept `mutable`.

type mutable Color {
    Red(),
    Green(),
    Blue(),
};

let main() Int = {
    let _ = Color.Red();
    return 0;
};
