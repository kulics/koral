// EXPECT: 3
// EXPECT: newline semicolon comment infix ok

let main() Int = {
    // Continuation is allowed across a comment-only line if the next token is a continuation token.
    let x = 1
    // comment blocks continuation
    + 2

    println(x)
    println("newline semicolon comment infix ok")
    return 0
}
