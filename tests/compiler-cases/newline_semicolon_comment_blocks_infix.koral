// EXPECT-ERROR: Unexpected token: +

let main() Int = {
    // Comments do not change ASI: `+` on the next physical line still starts a new statement.
    let x = 1
    // comment blocks continuation
    + 2
    return 0
}
