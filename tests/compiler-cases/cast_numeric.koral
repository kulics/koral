let main() Void = {
    let x Float64 = 42.0
    let y Int = x(Int)
    println(y)

    let a Int = 255
    let b UInt8 = a(UInt8)
    let c Int = b(Int)
    println(c)
}

// EXPECT: 42
// EXPECT: 255
