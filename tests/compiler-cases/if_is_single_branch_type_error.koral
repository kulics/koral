// Test that single-branch if...is bodies are statement-only, so break with value is rejected.
// EXPECT-ERROR: break with value outside of branch expression body

type MyOption {
    Some(value Int),
    None(),
}

let main() Int = {
    let opt = MyOption.Some(10)
    // This should fail: single-branch if...is body is statement-context only.
    if opt is .Some(v) then { break v }
    return 0
}
