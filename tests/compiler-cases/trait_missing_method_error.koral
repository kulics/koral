// EXPECT-ERROR: does not explicitly implement trait Eq

type Box(x Int)

let isSelfEqual[T Eq](x T) Bool = x == x

let main() Int = {
    let b = Box(1)
    let ok = isSelfEqual(b)
    return if ok then { yield 1 } else { yield 0 }
}
