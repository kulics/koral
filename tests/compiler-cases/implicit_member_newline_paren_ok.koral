// EXPECT: implicit member newline ok

type Flag {
    On(),
    Off(),
};

let make() Flag = .On
(
);

let main() Void = {
    when make() in {
        .On() then println("implicit member newline ok"),
        .Off() then println("bad"),
    };
};
