// EXPECT-ERROR: Cannot take '&mutable' of immutable value

type Counter(mutable value Int)

given Counter {
    inc(*mutable self) Void = {
        self.value = self.value + 1
    }
}

let main() Void = {
    let mutable c = Counter(1)
    let p unsafe * Counter = unsafe &c
    (unsafe *p).inc()
}