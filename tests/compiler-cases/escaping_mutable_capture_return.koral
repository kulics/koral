// EXPECT: 1
// EXPECT: 2

let make_counter() Func() Int = {
    let mutable x = 0;
    return () -> {
        x = x + 1;
        return x;
    };
};

let main() Int = {
    let counter = make_counter();
    println(counter());
    println(counter());
    return 0;
};