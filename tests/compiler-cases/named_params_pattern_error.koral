// EXPECT-ERROR: Field 'date' omitted by '...', but type 'Date' does not implement Default

using std::time { Date }

type NeedsDate(date Date, count Int)

let main() Int = {
    let _ = NeedsDate(count: 1, ...)
    return 0
}