// Tests that a non-trait given extension on T * outside std is rejected by orphan rule
// Feature: type-modifier-trait-conformance
// EXPECT-ERROR: Cannot declare

// Non-trait given on T * is only allowed in std module
given[T Any] * T {

    public identity(self) * T = self
}

let main() Void = {
    println("should not compile")
}
