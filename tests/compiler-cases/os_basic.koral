// EXPECT: args_ok

using std::os { .. }

let main() Void = {
    // ========================================================================
    // args() — should return at least the executable path
    // ========================================================================
    let arg_list = args()
    assert(arg_list.count() >= 1, "args count")
    println("args_ok")
}
