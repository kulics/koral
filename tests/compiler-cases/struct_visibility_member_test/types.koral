// types.koral - Defines structs with private fields for member access test

public type Secret(
    public x Int,
    private y Int
)

given Secret {
    public new(x Int, y Int) Secret = Secret(x, y)
    public get_y(self) Int = self.y
}
