// types.koral - Defines structs with file_private fields for member access test

public type Secret(
    public x Int,
    file_private y Int
);

given Secret {

    public new(x Int, y Int) Secret = Secret(x, y);
    public get_y(self) Int = self.y;
};
