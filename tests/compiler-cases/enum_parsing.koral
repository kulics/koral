// EXPECT-ERROR: Duplicate definition: Option

type TreeNode {
    Empty(),
    Node(value Int, left ref TreeNode, right ref TreeNode),
}

type Option[T Any] {
    Some(value T),
    None(),
}

let main() Int = {
    return 0
}
