// EXPECT: cleanup
// EXPECT: defer_tail_ok

let main() Int = {
    let value = if true then {
        defer println("cleanup");
        7
    } else {
        0
    };
    if value == 7 then {
        println("defer_tail_ok");
    };
    return 0;
};
