// EXPECT-ERROR: Named labels are only allowed on struct and enum constructors; use positional arguments

type DateLike(value Int)

given DateLike {
    public new(value Int) DateLike = DateLike(value)
}

let main() Int = {
    let _ = DateLike.new(value: 1)
    return 0
}