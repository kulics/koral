// EXPECT: created

type DateLike(value Int);

given DateLike {
    public new(value: Int) DateLike = DateLike(value);
};

let main() Int = {
    let _ = DateLike.new(value: 1);
    println("created");
    return 0;
};
