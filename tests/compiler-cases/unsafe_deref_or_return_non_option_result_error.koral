// EXPECT-ERROR: 'or return' can only be used with Option or Result types, got 'UInt8'

let take(ptr *unsafe UInt8) Int = {
    let _ = *ptr or return
    return 0
}

let main() Int = take(null_ptr())