// EXPECT-ERROR: Named labels are only allowed on struct and enum constructors; use positional arguments

let connect(host String, port Int) String = "connected"

let main() Int = {
    let _ = connect(host: "localhost", port: 8080)
    return 0
}
