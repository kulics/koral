// EXPECT-ERROR: Unexpected token: using

foreign using "m"

foreign type FILE {}
foreign let fclose(file unsafe * FILE) Int32
foreign let sin(x Float64) Float64
foreign let exit(status Int32) Never

let main() Void = {
    let mutable v Float64 = sin(0.0)
    v = sin(v)
}
