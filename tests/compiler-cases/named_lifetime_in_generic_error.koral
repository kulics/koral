// EXPECT-ERROR: Named lifetime parameters (e.g. 'a) are not supported in generic parameter lists

// Negative test: named lifetime parameters in generic lists should be rejected.

let foo['a](x ref 'a Int) Int = x

let main() Int = {
    return 0
}
