// EXPECT-ERROR: Lifetime parameters are not supported in generic parameter lists

// Negative test: lifetime parameters (both 'a and '_) in generic lists should be rejected.

let foo['a](x ref 'a Int) Int = x

let main() Int = {
    return 0
}
