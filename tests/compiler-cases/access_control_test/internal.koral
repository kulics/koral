// internal.koral - test module_private and public symbols

// Public symbol - accessible from anywhere
public let get_public_value() Int = 100

// Protected symbol - accessible from same module and submodules
module_private let get_protected_value() Int = 200

// Private symbol - only accessible in this file
file_private let private_helper() Int = 50
