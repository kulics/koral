// EXPECT: OK
// EXPECT: 100

type Button(width: Int, height: Int, label: String)

let main() Int = {
    let b = Button(width: 100, height: 50, label: "OK")
    println(b.label)
    println(b.width)
    return 0
}
