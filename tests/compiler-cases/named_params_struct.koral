// EXPECT: hello
// EXPECT: 0
// EXPECT: 0

type Window(title String, width Int, height Int)
type Counter(value Int)

let main() Int = {
    let w = Window(title: "hello", ...)
    let c = Counter(...)
    println(w.title)
    println(w.width)
    println(c.value)
    return 0
}
