// EXPECT-ERROR: Type mismatch: expected ref Int, got Int

// Negative test: passing a value to a function expecting ref T
// should fail without explicit .ref syntax.

let takes_ref(x ref Int) Int = x

let main() Int = {
    let x = 42
    let r = takes_ref(x)
    return r
}
