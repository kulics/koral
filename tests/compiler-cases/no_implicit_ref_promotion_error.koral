// EXPECT-ERROR: Type mismatch: expected *Int, got Int

// Negative test: passing a value to a function expecting * T
// should fail without explicit & syntax.

let takes_ref(x * Int) Int = x

let main() Int = {
    let x = 42
    let r = takes_ref(x)
    return r
}
