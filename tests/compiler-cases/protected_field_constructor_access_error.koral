// EXPECT-ERROR: Cannot directly construct type 'Rune' because field 'value' is protected. Use a public factory method instead.

let main() Int = {
    let r = Rune(65(UInt32))
    println(r.to_string())
    return 0
}
