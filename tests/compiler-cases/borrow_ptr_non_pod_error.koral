// EXPECT-ERROR: does not explicitly implement trait Pod

// `borrow_ptr` must not work as a generic "any Koral type -> *unsafe T" bridge:
// element types without `Pod` (managed runtime state) are rejected.

let main() Int = {
    let names = List[String].new();
    names.push("hello");
    let _ = names.borrow_ptr();
    return 0;
};
