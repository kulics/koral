// EXPECT-ERROR: file merge syntax no longer supports aliases

using "alpha" as Alpha

let main() Int = {
    let user Alpha.UserId = Alpha.UserId(7)
    println(user.value.to_string())
    return 0
}
