// Test trait object self-conformance: ToString * can be passed to [T ToString]
// EXPECT: hello world

let show[T ToString](a * T) String = a.to_string()

let main() Int = {
    let mut value = "hello world"
    let s1 * String = &value
    let s * ToString = s1
    println(show(s))
    return 0
}
