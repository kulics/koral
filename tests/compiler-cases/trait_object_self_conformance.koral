// Test trait object self-conformance: ToString ref can be passed to [T ToString]
// EXPECT: hello world

let show[T ToString](a ref T) String = a.to_string()

let main() Int = {
    let mut value = "hello world"
    let s1 = value.ref
    let s ref ToString = s1
    println(show(s))
    return 0
}
