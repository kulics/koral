// Test that old implicit block value code produces type mismatch error
// The block { 1 + 2 } without yield has type Void, not Int
// EXPECT-ERROR: Type mismatch

let main() Int = {
    // Old-style implicit block value — should fail
    let x Int = {
        let a = 1
        a + 2
    }
    yield x
}
