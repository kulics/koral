// EXPECT-ERROR: Cannot assign to immutable variable: x

type Vec(mutable x Int, mutable y Int)
type Holder(link * Vec)

let main() Int = {
    let h = Holder(box(Vec(3, 4)))
    h.link.x = 30
    return 0
}