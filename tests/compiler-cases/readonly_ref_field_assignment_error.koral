// EXPECT-ERROR: Cannot assign to immutable variable: x

type Vec(mut x Int, mut y Int)
type Holder(link Vec ref)

let main() Int = {
    let h = Holder(box(Vec(3, 4)))
    h.link.x = 30
    yield 0
}