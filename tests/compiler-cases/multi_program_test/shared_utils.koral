// shared_utils.koral - Shared utility functions for multiple programs

public let add(a Int, b Int) Int = a + b

public let multiply(a Int, b Int) Int = a * b

public type Point(x Int, y Int)

public let create_point(x Int, y Int) Point = Point(x, y)
