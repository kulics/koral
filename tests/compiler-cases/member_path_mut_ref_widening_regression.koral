// EXPECT: ok

type Big(value Int)
given Big not Deref

type Holder(mut value * Big)

let read_value(holder *mut Holder) * Big = holder.value

let main() Void = {
    let big *mut Big = box(Big(7))
    let holder *mut Holder = box(Holder(big))
    let value * Big = read_value(holder)
    assert(value.value == 7, "mutable field * should widen to readonly *")
    println("ok")
}
