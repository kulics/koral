// EXPECT: for_expr_void_ok

let main() Int = {
    let nums = [1, 2, 3];
    let loop_value Void = for n in nums then {
        let _ = n;
    };
    let _ = loop_value;
    println("for_expr_void_ok");
    return 0;
};