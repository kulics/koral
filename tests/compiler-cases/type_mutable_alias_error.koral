// EXPECT-ERROR: Type alias cannot be marked mutable

// Declaration-site mutability is only valid on nominal types.
// Type aliases must not accept `mutable`.

type mutable Meters = Int;

let main() Int = {
    let _ = 0;
    return 0;
};
