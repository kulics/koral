// Test or return error outside function scope
// EXPECT-ERROR: 'or return' can only be used inside a function

private let bad [Int]Option = [Int]Option.Some(1) or return

let main() Int = {
    yield 0
}