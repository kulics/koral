// EXPECT-ERROR: compiler protocol method drop cannot be called explicitly

type Token();

given Token as Drop {

    drop(self) Void = {};
};

let main() Int = {
    let mutable a = Token();
    a.drop();
    return 0;
};
