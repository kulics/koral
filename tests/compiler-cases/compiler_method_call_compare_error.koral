// EXPECT-ERROR: Member 'drop' not found in type 'Token'

type Token()

given Token as Drop {

    drop(source ptr mut Self) Void = {}
}

let main() Int = {
    let mut a = Token()
    a.drop()
    return 0
}
