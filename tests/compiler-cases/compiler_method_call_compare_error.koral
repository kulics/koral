// EXPECT-ERROR: Member 'drop' not found in type 'Token'

type Token()

given Token as Drop {

    drop(source *unsafe mutable Self) Void = {}
}

let main() Int = {
    let mutable a = Token()
    a.drop()
    return 0
}
