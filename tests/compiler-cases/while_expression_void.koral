// EXPECT: while_expr_void_ok

let main() Int = {
    let mutable count = 0;
    let loop_value Void = while count < 3 then {
        count += 1;
    };
    let _ = loop_value;
    println("while_expr_void_ok");
    return 0;
};