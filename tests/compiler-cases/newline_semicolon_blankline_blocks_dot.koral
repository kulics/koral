// EXPECT: 3
// EXPECT: newline semicolon blankline dot ok

let main() Int = {
    // Continuation is allowed across a blank line if the next token is a continuation token.
    let n = "abc"

    .count()

    println(n)
    println("newline semicolon blankline dot ok")
    return 0
}
