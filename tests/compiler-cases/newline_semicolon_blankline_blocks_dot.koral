// EXPECT: 3
// EXPECT: newline semicolon blankline dot ok

let main() Int = {
    // Blank lines still allow the explicit `.` join point.
    let n = "abc"

    .count()

    println(n)
    println("newline semicolon blankline dot ok")
    return 0
}
