// EXPECT-ERROR: Cannot use implicit member expression '.count' without a known type context

let main() Int = {
    // Continuation is NOT allowed across a blank line.
    let n = "abc"

    .count()

    // Unreachable
    println(n)
    yield 0
}
