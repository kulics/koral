// EXPECT-ERROR: Cannot call '*mut self' method 'increment' on an rvalue; store the value in a 'let mut' variable first

type Counter(mut value Int)

given Counter {

    increment(*mut self) Void = {
        self.value = self.value + 1
    }
}

let main() Int = {
    Counter(1).increment()
    return 0
}