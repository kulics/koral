// EXPECT-ERROR: Expression statement has no effect

let main() Int = {
    let value Int = if true then {
        let inner = if true then {
            break 1
        } else {
            break 2
        }
        let _ = inner
    } else {
        3
    }
    return value
}
