// EXPECT-ERROR: Collection subscript results are values and cannot be used as writable base addresses

using std { .. };

type Point(x Int, y Int);

let main() Void = {
    let mutable points = List[Point].new();
    points.push(Point(1, 2));
    points[0].y = 77;
};