// private using 只应在声明文件内可见，不应泄漏到同模块其他文件
// EXPECT-ERROR: Import it explicitly with using Std::Time { DateTime }.

using "imports"
using "consumer"

public let main() Int = {
	let _ = trigger_private_using_leak()
	return 0
}
