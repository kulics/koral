// EXPECT: 7

trait CounterOps {
    peek(*self) Int
}

type Counter(value Int)

given Counter as CounterOps {

    public peek(*self) Int = self.value
}

let choose(flag Bool, left *mut CounterOps, right * CounterOps) * CounterOps =
    if flag then left else right

let main() Int = {
    let strong *mut Counter = box(Counter(7))
    let left *mut CounterOps = strong
    let right * CounterOps = left
    let chosen = choose(true, left, right)
    println(chosen.peek().to_string())
    return 0
}