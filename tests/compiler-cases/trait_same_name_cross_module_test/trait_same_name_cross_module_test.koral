using trait_same_name_cross_module_test::alpha { alpha_value };
using trait_same_name_cross_module_test::beta { beta_value };

// EXPECT: 11
// EXPECT: 22

let main() Int = {
    println(alpha_value().to_string());
    println(beta_value().to_string());
    return 0;
};