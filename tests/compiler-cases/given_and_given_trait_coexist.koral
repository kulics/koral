// EXPECT: true
// EXPECT: true

type Coord(x Int, y Int)

given Coord {
    equals(self, other Coord) Bool = self.x == other.x and self.y == other.y
}

given Coord Eq {
    equals(self, other Coord) Bool = self.x == other.x and self.y == other.y
}

let main() Int = {
    let a = Coord(1, 2)
    let b = Coord(1, 2)
    println(a.equals(b).to_string())
    println((a == b).to_string())
    yield 0
}
