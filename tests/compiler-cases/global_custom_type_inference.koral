// Test: global variable with custom type initialization and type inference

type Point(x Int, y Int)
type Color { Red(), Green(), Blue() }
type Wrapper(value Int)

private let origin = Point(0, 0)
private let red = Color.Red()
private let wrapped = Wrapper(42)
private let mutable cursor = Point(10, 20)

public let main() Void = {
    println(origin.x)
    println(origin.y)
    println(wrapped.value)
    println(cursor.x)
    cursor = Point(30, 40)
    println(cursor.x)
    println(cursor.y)
}
// EXPECT: 0
// EXPECT: 0
// EXPECT: 42
// EXPECT: 10
// EXPECT: 30
// EXPECT: 40
