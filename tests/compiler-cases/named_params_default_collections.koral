// EXPECT: 0
// EXPECT: 0

type Config(items: List[Int] = [], flags: Dict[String, Int] = []);

let main() Int = {
    let c = Config();
    println(c.items.count());
    println(c.flags.count());
    return 0;
};
