// EXPECT-ERROR: Type mismatch: expected ref mut Counter, got Counter

type Counter(mut value Int)

given Counter {
    public increment(self ref mut Self) Void = {
        self.value += 1
    }
}

let main() Int = {
    let mut c = Counter(0)
    c.increment()
    return c.value
}