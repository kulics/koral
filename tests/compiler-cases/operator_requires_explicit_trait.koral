// EXPECT-ERROR: does not explicitly implement trait Add

type Span(ms Int)

given Span {
    add(self, other Span) Span = Span(self.ms + other.ms)
}

let main() Int = {
    let a = Span(1)
    let b = Span(2)
    let _ = a + b
    yield 0
}
