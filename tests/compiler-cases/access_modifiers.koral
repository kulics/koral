file_private let x Int = 10;

public let foo() Void = {
  println(x);
};

module_private type Point(
  public x Int,
  file_private y Int
);

given Point {

    public get_y(self) Int = {
        return self.y;
    };
};

public let main() Void = {
    foo();
};
// EXPECT: 10
