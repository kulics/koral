// EXPECT-ERROR: Unknown named argument 'wrong' for 'scale'

// Labels on free function calls must be validated, not silently dropped.

let scale(value Int, factor: Int) Int = value * factor;

let main() Int = {
    return scale(10, wrong: 3);
};
