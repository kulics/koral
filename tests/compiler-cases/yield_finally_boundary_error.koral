// EXPECT-ERROR: control flow statement 'yield' is not allowed in finally expression

let main() Int = {
    let value Int = if true then {
        finally {
            yield 1
        }
        yield 2
    } else {
        yield 3
    }
    return value
}
