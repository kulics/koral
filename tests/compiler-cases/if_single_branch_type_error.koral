// Test that single-branch if expressions still require a Void/Never then-branch.
// EXPECT-ERROR: Single-branch 'if' must have Void or Never type in then branch

let main() Int = {
    let x = true;
    let value = if x then { 42 };
    let _ = value;
    return 0;
};
