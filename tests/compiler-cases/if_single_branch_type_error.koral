// Test that single-branch if bodies are statement-only, so yield is rejected.
// EXPECT-ERROR: yield outside of branch expression body

let main() Int = {
    let x = true
    // This should fail: single-branch if body is statement-context only.
    if x then { yield 42 }
    return 0
}
