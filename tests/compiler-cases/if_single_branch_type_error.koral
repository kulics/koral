// Test that single-branch if bodies are statement-only, so break with value is rejected.
// EXPECT-ERROR: break with value outside of branch expression body

let main() Int = {
    let x = true
    // This should fail: single-branch if body is statement-context only.
    if x then { break 42 }
    return 0
}
