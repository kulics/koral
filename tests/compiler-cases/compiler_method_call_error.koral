// EXPECT-ERROR: Member 'drop' not found in type 'Token'

type Token()

given Token as Drop {

    drop(source *! mutable Self) Void = {}
}

let main() Int = {
    let mutable a = Token()
    // Compiler protocol methods (like drop) must not be callable explicitly.
    a.drop()
    return 0
}
