// EXPECT-ERROR: compiler protocol method drop cannot be called explicitly

type Token();

given Token as Drop {

    drop(self) Void = {};
};

let main() Int = {
    let mutable a = Token();
    // Compiler protocol methods (like drop) must not be callable explicitly.
    a.drop();
    return 0;
};
