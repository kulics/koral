// Test case for indirect recursion detection (two types)

// Two types that mutually reference each other by value
// This creates an infinite-size type and should be rejected
type A(b B);
type B(a A);

let main() Int = 0;
