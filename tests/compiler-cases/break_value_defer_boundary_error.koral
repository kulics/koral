// EXPECT-ERROR: control flow statement 'break' is not allowed in defer expression

let main() Int = {
    let value Int = if true then {
        defer {
            break 1
        }
        break 2
    } else {
        break 3
    }
    return value
}
