// EXPECT-ERROR: control flow statement 'yield' is not allowed in defer expression

let main() Int = {
    let value Int = if true then {
        defer {
            yield 1
        }
        yield 2
    } else {
        yield 3
    }
    return value
}
