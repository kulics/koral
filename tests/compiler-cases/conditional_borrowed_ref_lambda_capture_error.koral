// EXPECT-ERROR: Cannot capture borrowed reference

let choose_getter(flag Bool) Func[Int] = {
    let mut left = 11
    let mut right = 22
    let chosen = if flag then left.ref else right.ref
    return () -> chosen.val
}

let main() Int = {
    let _ = choose_getter(true)
    return 0
}