// EXPECT: cast-copy-refcount: 3

type Holder(value Int ref)

let main() Int = {
    let mut v = 1
    let r = v.ref
    let a = Holder(r)
    let b Holder = (Holder)a

    assert(b.value.val == 1, "copied holder value")
    print("cast-copy-refcount: ")
    println(ref_count(r))
    return 0
}
