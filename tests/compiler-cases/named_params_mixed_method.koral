// EXPECT: 15

type Calc(base Int);

given Calc {
    public add(self, value Int, multiplier: Int = 1) Int = (self.base + value) * multiplier;
};

let main() Int = {
    let c = Calc(10);
    println(c.add(5, multiplier: 1));
    return 0;
};
