// EXPECT-ERROR: cannot take ref of literal

type Box(inner ref Int)

let getBox() Box = {
    let r ref Int = 100.ref
    return Box(r)
}

let main() Int = {
    let b = getBox()
    println(b.inner.val)
    return 0
}
