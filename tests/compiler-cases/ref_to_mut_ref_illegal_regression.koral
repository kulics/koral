// EXPECT-ERROR: Type mismatch: expected *mutable Boxed, got *Boxed

type Boxed(value Int)

let main() Void = {
    let mr *mutable Boxed = box(Boxed(7))
    let r * Boxed = mr
    let _ *mutable Boxed = r
}
