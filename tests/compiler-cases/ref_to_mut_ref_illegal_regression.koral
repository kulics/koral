// EXPECT-ERROR: Type mismatch: expected mut ref Boxed, got ref Boxed

type Boxed(value Int)

let main() Void = {
    let mr mut ref Boxed = box(Boxed(7))
    let r ref Boxed = mr
    let _ mut ref Boxed = r
}
