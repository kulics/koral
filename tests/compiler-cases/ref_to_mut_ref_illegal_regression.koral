// EXPECT-ERROR: Type mismatch: expected ref mut Boxed, got ref Boxed

type Boxed(value Int)

let main() Void = {
    let mr ref mut Boxed = box(Boxed(7))
    let r ref Boxed = mr
    let _ ref mut Boxed = r
}
