// EXPECT-ERROR: Type mismatch: expected Boxed mut ref, got Boxed ref

type Boxed(value Int)

let main() Void = {
    let mr Boxed mut ref = box(Boxed(7))
    let r Boxed ref = mr
    let _ Boxed mut ref = r
}
