// EXPECT-ERROR: Type mismatch

let main() Int = {
    let value Int = if true then {
        let inner = if true then {
            yield 1
        } else {
            yield 2
        }
        let _ = inner
    } else {
        3
    }
    return value
}
