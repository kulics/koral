// EXPECT-ERROR: subscript is only supported for String, List, Deque, Dict, and pointer types

type Box(v Int)

given Box {

    at(self, idx Int) Int = self.v
}

let main() Int = {
    let b = Box(7)
    let _ = b[0]
    return 0
}
