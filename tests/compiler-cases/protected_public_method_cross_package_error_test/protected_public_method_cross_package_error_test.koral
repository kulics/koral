// EXPECT-ERROR: Cannot directly construct type 'RemoteBox' because field 'value' is protected public. Use a public factory method instead.

using dep_models { RemoteBox }

public let main() Int = {
    let box = RemoteBox(7)
    println(box.value.to_string())
    return 0
}