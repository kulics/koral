// EXPECT-ERROR: Enum case name 'some' must start with an uppercase letter

let main() Void = {
    let value Option[Int] = .Some(1)
    when value in {
        .some(inner) then {
            let _ = inner
        },
        .None then {},
    }
}