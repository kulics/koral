// EXPECT-ERROR: Cannot use unsafe * on an opaque type unsafe *: type layout is unknown at compile time

foreign type FILE {}

let main() Void = {
    let p unsafe * FILE = null_ptr()
    let f FILE = unsafe *p
}
