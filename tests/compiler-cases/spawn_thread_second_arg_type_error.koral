// EXPECT-ERROR: spawn_thread: second argument must be *! mut UInt64

let main() Int = {
    let out_handle *! mut *! UInt8 = null_ptr()
    let wrong *! UInt64 = null_ptr()
    spawn_thread(out_handle, wrong, () -> {}, 0(UInt64))
    return 0
}