// EXPECT-ERROR: spawn_thread: second argument must be unsafe * mutable UInt64

let main() Int = {
    let out_handle unsafe * mutable unsafe * UInt8 = null_ptr()
    let wrong unsafe * UInt64 = null_ptr()
    spawn_thread(out_handle, wrong, () -> {}, 0(UInt64))
    return 0
}