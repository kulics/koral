// EXPECT-ERROR: spawn_thread: second argument must be *raw mut UInt64

let main() Int = {
    let out_handle *raw mut *raw UInt8 = null_ptr()
    let wrong *raw UInt64 = null_ptr()
    spawn_thread(out_handle, wrong, () -> {}, 0(UInt64))
    return 0
}